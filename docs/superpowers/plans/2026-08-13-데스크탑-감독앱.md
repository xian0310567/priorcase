# priorcase 데스크탑 감독 앱 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 기계가 한 일(자동 기록)을 사람이 클릭 한 번으로 승인·정정·판정하는 macOS 메뉴바 상주 앱을 만든다.

**Architecture:** Tauri 2 앱. Rust 측은 `prior` 바이너리를 자식 프로세스로 실행하고 stdout(JSON)을 그대로 웹뷰로 넘긴다. **앱은 볼트 파일을 절대 직접 읽거나 쓰지 않는다** — frontmatter 방출·스키마 검증·색인 갱신·볼트 선택이 두 벌이 되면 한쪽만 고쳐진 채로 남는다. UI 는 순수 TypeScript + Vite(프레임워크 없음): 화면 넷에 상태가 거의 없고, 프레임워크를 얹으면 이 앱에서 가장 큰 의존성이 된다.

**Tech Stack:** Tauri 2 · Rust(2021 edition) · TypeScript · Vite · pnpm · 외부 UI 라이브러리 없음

## Global Constraints

이 목록은 모든 태스크의 요구사항에 암묵적으로 포함된다.

- **앱은 `prior` 명령만 부른다.** 볼트 경로를 조립하거나 `.md`·`state.json`·`promotions.jsonl` 을 직접 읽는 코드는 리뷰에서 거부한다.
- **빈 큐와 실패를 구별한다.** 이 시스템은 모든 부품이 실패해도 대화를 막지 않도록 설계됐고, 그 대가로 고장이 정상과 구별되지 않는다. 앱이 그 구별을 못 하면 앱도 같은 병에 걸린다.
- **푸시 알림을 보내지 않는다.** 트레이 아이콘의 숫자만 바꾼다.
- **`prior` 경로는 설정 가능해야 한다.** 기본은 `PATH` 탐색, 없으면 `~/.local/bin/prior`. 테스트는 이 경로를 가짜 스크립트로 바꿔 돌린다.
- **모든 CLI 호출에 타임아웃을 건다.** `queue` 는 10초, 쓰기 명령은 90초(`prior promote` 가 판별기를 부르고 그 예산이 90초다).
- **UI 문자열은 한국어다.** 볼트와 CLI 진단이 한국어이므로 섞이면 읽기 나쁘다.
- **v1 은 애플 공증을 하지 않는다.** `tauri build` 로 `.app` 까지만 만든다 — DMG 번들링은 비대화형 세션에서 멈춘다([[omni-결정-Tauri빌드-DMG헤드리스제약-2026-08-01]]).
- **작업 디렉토리는 앱 번들 밖이어야 한다.** `prior` 는 cwd 로 볼트를 고르는 명령이 있는데(`recall`), 앱이 부르는 명령은 전부 cwd 에 의존하지 않는다. 그래도 자식 프로세스의 cwd 는 `$HOME` 으로 고정한다 — 앱 번들 안이면 `.app` 이 읽기 전용일 때 이상하게 실패한다.

---

## 파일 구조

```
app/                                  ← 새 디렉토리. Go 코드와 섞지 않는다
  package.json                        pnpm 워크스페이스 루트
  vite.config.ts
  index.html
  src/
    main.ts                           앱 진입점 · 폴링 루프 · 화면 전환
    types.ts                          queue --json 의 계약 타입 (수기, 생성 아님)
    api.ts                            Tauri invoke 래퍼. 여기만 백엔드를 안다
    render/
      confirm.ts                      확인 큐 화면
      review.ts                       검토 큐 화면
      retro.ts                        회고 큐 화면
      health.ts                       상태 화면
      shell.ts                        탭·배너·빈 상태 (화면 넷이 공유)
    format.ts                         날짜·볼트 라벨·발췌 접기 (순수 함수)
    style.css
  src-tauri/
    Cargo.toml
    tauri.conf.json
    build.rs
    src/
      main.rs                         트레이 · 창 · 커맨드 등록
      prior.rs                        prior 실행 · 타임아웃 · 오류 분류
      commands.rs                     #[tauri::command] 넷
    icons/                            트레이 아이콘 (템플릿 이미지)
  tests/
    format.test.ts                    순수 함수 단위 테스트
    render.test.ts                    화면이 빈 상태·오류를 어떻게 그리나
  fixtures/
    fake-prior-ok.sh                  정상 JSON 을 뱉는 가짜
    fake-prior-broken-json.sh         깨진 JSON
    fake-prior-exit1.sh               0 아닌 종료 + stderr
    fake-prior-warnings.sh            warnings 가 있는 JSON
    fake-prior-slow.sh                타임아웃 재현
```

**왜 `src-tauri/src` 를 셋으로 나누나.** `main.rs` 는 트레이·창 배선, `prior.rs` 는 프로세스 실행과 오류 분류, `commands.rs` 는 IPC 경계다. 셋이 한 파일에 있으면 "프로세스를 어떻게 부르나" 를 고칠 때 트레이 코드까지 읽어야 한다.

**왜 `render/` 를 화면별로 나누나.** 화면 넷이 서로 다른 데이터와 버튼을 갖고, 한 화면을 고칠 때 다른 화면을 안 읽어도 되게 한다. 공유하는 것(탭·배너·빈 상태)만 `shell.ts` 에 둔다.

---

## Task 1: Tauri 2 스캐폴딩과 트레이

**Files:**
- Create: `app/package.json`
- Create: `app/vite.config.ts`
- Create: `app/index.html`
- Create: `app/src/main.ts`
- Create: `app/src/style.css`
- Create: `app/src-tauri/Cargo.toml`
- Create: `app/src-tauri/tauri.conf.json`
- Create: `app/src-tauri/build.rs`
- Create: `app/src-tauri/src/main.rs`
- Create: `app/.gitignore`
- Modify: `.gitignore` (레포 루트)

**Interfaces:**
- Produces: `app/` 워크스페이스. `pnpm --dir app tauri dev` 로 메뉴바에 아이콘이 뜬다.

- [ ] **Step 1: pnpm 프로젝트를 만든다**

`app/package.json`:

```json
{
  "name": "priorcase-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0",
    "vitest": "^3.2.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.9.0"
  }
}
```

`app/vite.config.ts`:

```ts
import { defineConfig } from "vite";

export default defineConfig({
  // Tauri 가 이 포트를 고정으로 기대한다. 바꾸면 tauri.conf.json 도 같이 바꿔야 한다.
  server: { port: 5173, strictPort: true },
  build: { outDir: "dist", emptyOutDir: true },
});
```

`app/index.html`:

```html
<!doctype html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <title>priorcase</title>
    <link rel="stylesheet" href="/src/style.css" />
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

`app/src/style.css`:

```css
:root { color-scheme: light dark; font-family: -apple-system, system-ui, sans-serif; }
body { margin: 0; font-size: 13px; }
#app { padding: 12px; min-width: 420px; }
```

`app/src/main.ts`:

```ts
document.querySelector<HTMLDivElement>("#app")!.textContent = "priorcase";
```

`app/.gitignore`:

```
node_modules/
dist/
src-tauri/target/
```

- [ ] **Step 2: 레포 루트 .gitignore 에 앱 산출물을 더한다**

`.gitignore` 맨 아래에 붙인다:

```
# 데스크탑 앱
/app/node_modules/
/app/dist/
/app/src-tauri/target/
```

- [ ] **Step 3: Rust 쪽을 만든다**

`app/src-tauri/Cargo.toml`:

```toml
[package]
name = "priorcase-app"
version = "0.1.0"
edition = "2021"
rust-version = "1.77"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
strip = true
```

`app/src-tauri/build.rs`:

```rust
fn main() {
    tauri_build::build()
}
```

`app/src-tauri/tauri.conf.json`:

```json
{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "priorcase",
  "version": "0.1.0",
  "identifier": "dev.priorcase.app",
  "build": {
    "beforeDevCommand": "pnpm dev",
    "devUrl": "http://localhost:5173",
    "beforeBuildCommand": "pnpm build",
    "frontendDist": "../dist"
  },
  "app": {
    "windows": [
      {
        "title": "priorcase",
        "width": 460,
        "height": 620,
        "resizable": true,
        "visible": false,
        "decorations": true,
        "skipTaskbar": true
      }
    ],
    "security": { "csp": "default-src 'self'" },
    "macOSPrivateApi": false
  },
  "bundle": {
    "active": true,
    "targets": ["app"],
    "icon": ["icons/icon.png"],
    "macOS": { "minimumSystemVersion": "11.0" }
  }
}
```

`bundle.targets` 가 `["app"]` 인 이유: DMG 번들링이 비대화형 세션에서 멈춘다.

- [ ] **Step 4: 트레이 아이콘 파일을 만든다**

```bash
cd /Users/zesty/project/casebook/app/src-tauri
mkdir -p icons
# 32x32 투명 PNG. 템플릿 이미지라 단색이면 된다.
python3 - <<'PY'
import struct, zlib
w = h = 32
rows = []
for y in range(h):
    row = b"\x00"
    for x in range(w):
        # 가운데에 채워진 원 (반지름 12)
        d = ((x - 15.5) ** 2 + (y - 15.5) ** 2) ** 0.5
        a = 255 if d < 12 else 0
        row += bytes((0, 0, 0, a))
    rows.append(row)
raw = b"".join(rows)
def chunk(tag, data):
    c = tag + data
    return struct.pack(">I", len(data)) + c + struct.pack(">I", zlib.crc32(c))
png = (b"\x89PNG\r\n\x1a\n"
       + chunk(b"IHDR", struct.pack(">IIBBBBB", w, h, 8, 6, 0, 0, 0))
       + chunk(b"IDAT", zlib.compress(raw))
       + chunk(b"IEND", b""))
open("icons/icon.png", "wb").write(png)
print("icons/icon.png", len(png), "bytes")
PY
```

Expected: `icons/icon.png 249 bytes` 안팎

- [ ] **Step 5: 트레이와 창 토글을 붙인다**

`app/src-tauri/src/main.rs`:

```rust
// Windows 릴리스에서 콘솔 창이 뜨지 않게 한다. macOS 에서는 무해하다.
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

use tauri::{
    menu::{Menu, MenuItem},
    tray::TrayIconBuilder,
    Manager,
};

fn main() {
    tauri::Builder::default()
        .setup(|app| {
            let quit = MenuItem::with_id(app, "quit", "종료", true, None::<&str>)?;
            let menu = Menu::with_items(app, &[&quit])?;

            TrayIconBuilder::with_id("main")
                .icon(app.default_window_icon().unwrap().clone())
                // 템플릿 이미지로 두면 macOS 가 라이트/다크에 맞춰 색을 뒤집는다.
                .icon_as_template(true)
                .menu(&menu)
                // 왼쪽 클릭은 메뉴가 아니라 창을 연다 — 메뉴는 오른쪽 클릭이다.
                .show_menu_on_left_click(false)
                .on_menu_event(|app, event| {
                    if event.id() == "quit" {
                        app.exit(0);
                    }
                })
                .on_tray_icon_event(|tray, event| {
                    use tauri::tray::{MouseButton, MouseButtonState, TrayIconEvent};
                    if let TrayIconEvent::Click {
                        button: MouseButton::Left,
                        button_state: MouseButtonState::Up,
                        ..
                    } = event
                    {
                        let app = tray.app_handle();
                        if let Some(w) = app.get_webview_window("main") {
                            if w.is_visible().unwrap_or(false) {
                                let _ = w.hide();
                            } else {
                                let _ = w.show();
                                let _ = w.set_focus();
                            }
                        }
                    }
                })
                .build(app)?;
            Ok(())
        })
        .run(tauri::generate_context!())
        .expect("앱을 실행할 수 없다");
}
```

- [ ] **Step 6: 의존성을 받고 컴파일이 되는지 본다**

```bash
cd /Users/zesty/project/casebook/app
pnpm install
pnpm exec tsc --noEmit
cargo check --manifest-path src-tauri/Cargo.toml
```

Expected: 셋 다 에러 없이 끝난다. `cargo check` 는 첫 실행에서 수백 개 크레이트를 받으므로 몇 분 걸린다.

- [ ] **Step 7: 눈으로 확인한다**

```bash
cd /Users/zesty/project/casebook/app
pnpm tauri dev
```

Expected: 메뉴바에 검은 원 아이콘이 뜬다. 왼쪽 클릭하면 `priorcase` 라고만 적힌 창이 열리고 다시 누르면 닫힌다. 오른쪽 클릭하면 `종료` 메뉴가 나온다. Dock 에는 아이콘이 없다(`skipTaskbar`).

확인했으면 `Ctrl-C` 로 끝낸다.

- [ ] **Step 8: 커밋**

```bash
cd /Users/zesty/project/casebook
git add app .gitignore
git commit -m "feat(app): Tauri 2 스캐폴딩과 메뉴바 트레이

창은 기본으로 숨어 있고 트레이 왼쪽 클릭으로 토글한다. Dock 에는 안 뜬다 —
상주하는 감독 표면이지 창을 여는 앱이 아니다.

bundle.targets 를 [\"app\"] 으로 둔다. DMG 번들링이 비대화형 세션에서 멈추고
(omni 실측) v1 은 공증도 하지 않는다."
```

---

## Task 2: `prior` 실행과 오류 분류 (Rust)

**Files:**
- Create: `app/src-tauri/src/prior.rs`
- Modify: `app/src-tauri/src/main.rs` (모듈 선언 추가)
- Create: `app/fixtures/fake-prior-ok.sh`
- Create: `app/fixtures/fake-prior-broken-json.sh`
- Create: `app/fixtures/fake-prior-exit1.sh`
- Create: `app/fixtures/fake-prior-slow.sh`

**Interfaces:**
- Produces: `prior::run(bin: &str, args: &[&str], timeout: Duration) -> Result<String, PriorError>`
  where `enum PriorError { NotFound, Failed { code: Option<i32>, stderr: String }, Timeout, Io(String) }`

- [ ] **Step 1: 가짜 prior 스크립트 넷을 만든다**

```bash
cd /Users/zesty/project/casebook/app
mkdir -p fixtures

cat > fixtures/fake-prior-ok.sh <<'SH'
#!/bin/sh
# queue --json 이 내는 것과 같은 모양. 빈 큐 + 상태 하나.
cat <<'JSON'
{"confirm":[],"review":[],"retro":[],"health":[{"name":"볼트","level":"ok","detail":"/tmp/v"}]}
JSON
SH

cat > fixtures/fake-prior-broken-json.sh <<'SH'
#!/bin/sh
printf '{"confirm":[ this is not json'
SH

cat > fixtures/fake-prior-exit1.sh <<'SH'
#!/bin/sh
echo "설정을 읽을 수 없다 (/x/config.toml)" >&2
exit 1
SH

cat > fixtures/fake-prior-slow.sh <<'SH'
#!/bin/sh
sleep 30
echo '{}'
SH

chmod +x fixtures/*.sh
ls -l fixtures/
```

Expected: 네 파일이 `-rwxr-xr-x` 로 보인다.

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`app/src-tauri/src/prior.rs` 맨 아래에 넣을 테스트다. 먼저 이것만 쓴다:

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::time::Duration;

    fn fixture(name: &str) -> String {
        // src-tauri/ 기준으로 ../fixtures/
        let mut p = std::path::PathBuf::from(env!("CARGO_MANIFEST_DIR"));
        p.pop();
        p.push("fixtures");
        p.push(name);
        p.to_string_lossy().into_owned()
    }

    #[test]
    fn 정상_출력을_그대로_준다() {
        let out = run(&fixture("fake-prior-ok.sh"), &["queue", "--json"], Duration::from_secs(5))
            .expect("성공해야 한다");
        assert!(out.contains("\"confirm\""), "출력이 이상하다: {out}");
    }

    #[test]
    fn 없는_바이너리는_NotFound_다() {
        let err = run("/없는/자리/prior", &["queue"], Duration::from_secs(5))
            .expect_err("실패해야 한다");
        assert!(matches!(err, PriorError::NotFound), "err={err:?}");
    }

    #[test]
    fn 종료코드가_0이_아니면_stderr_를_담는다() {
        let err = run(&fixture("fake-prior-exit1.sh"), &["queue"], Duration::from_secs(5))
            .expect_err("실패해야 한다");
        match err {
            PriorError::Failed { code, stderr } => {
                assert_eq!(code, Some(1));
                // **stderr 를 버리면 안 된다.** 사람이 고칠 유일한 단서다.
                assert!(stderr.contains("설정을 읽을 수 없다"), "stderr={stderr}");
            }
            other => panic!("Failed 여야 한다: {other:?}"),
        }
    }

    #[test]
    fn 상한을_넘으면_Timeout_이다() {
        let err = run(&fixture("fake-prior-slow.sh"), &["queue"], Duration::from_millis(300))
            .expect_err("실패해야 한다");
        assert!(matches!(err, PriorError::Timeout), "err={err:?}");
    }
}
```

- [ ] **Step 3: 컴파일이 실패하는 것을 본다**

```bash
cd /Users/zesty/project/casebook/app/src-tauri
cargo test --lib 2>&1 | tail -5
```

Expected: `cannot find function 'run'` 같은 에러. 아직 구현이 없다.

- [ ] **Step 4: 최소 구현을 쓴다**

`app/src-tauri/src/prior.rs` 의 테스트 위에 넣는다:

```rust
use std::io::Read;
use std::process::{Command, Stdio};
use std::sync::mpsc;
use std::time::Duration;

/// PriorError 는 앱이 **서로 다르게 그려야 하는** 실패들이다.
///
/// 한 종류로 뭉치면 "설치가 안 됐다" 와 "설정이 깨졌다" 와 "느리다" 가 같은
/// 화면이 되고, 사람은 무엇을 고쳐야 할지 모른다.
#[derive(Debug)]
pub enum PriorError {
    /// 바이너리 자체가 없다 → 설치 안내 화면
    NotFound,
    /// 돌았지만 0 아닌 코드로 끝났다 → stderr 를 그대로 보여 준다
    Failed { code: Option<i32>, stderr: String },
    /// 상한을 넘겼다 → "느리다" 는 별개 사실이다
    Timeout,
    /// 그 밖 (파이프 실패 등)
    Io(String),
}

impl std::fmt::Display for PriorError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            PriorError::NotFound => write!(f, "prior 를 찾을 수 없다"),
            PriorError::Failed { code, stderr } => {
                write!(f, "prior 가 실패했다 (코드 {code:?}): {stderr}")
            }
            PriorError::Timeout => write!(f, "prior 가 상한 안에 끝나지 않았다"),
            PriorError::Io(m) => write!(f, "prior 를 실행할 수 없다: {m}"),
        }
    }
}

/// run 은 prior 를 한 번 부르고 stdout 을 준다.
///
/// **cwd 를 $HOME 으로 고정한다.** 앱 번들 안에서 돌면 `.app` 이 읽기 전용일 때
/// 이상하게 실패하고, 무엇보다 prior 의 일부 명령이 cwd 로 볼트를 고른다 —
/// 앱이 부르는 명령은 그렇지 않지만, 우연히 그런 명령을 추가했을 때 앱 번들
/// 경로가 도메인 해석에 들어가는 일을 막는다.
pub fn run(bin: &str, args: &[&str], timeout: Duration) -> Result<String, PriorError> {
    let home = std::env::var("HOME").unwrap_or_else(|_| "/".to_string());
    let mut child = match Command::new(bin)
        .args(args)
        .current_dir(home)
        .stdout(Stdio::piped())
        .stderr(Stdio::piped())
        .spawn()
    {
        Ok(c) => c,
        Err(e) if e.kind() == std::io::ErrorKind::NotFound => return Err(PriorError::NotFound),
        Err(e) => return Err(PriorError::Io(e.to_string())),
    };

    // stdout/stderr 를 먼저 스레드로 빨아들인다. **파이프를 안 비우면 자식이
    // 버퍼가 차서 멈추고, 그러면 타임아웃이 "느린 명령" 이 아니라 우리가 만든
    // 교착이 된다.** prior queue 의 출력이 수백 KB 가 될 수 있다.
    let mut so = child.stdout.take().expect("stdout 파이프");
    let mut se = child.stderr.take().expect("stderr 파이프");
    let (tx_o, rx_o) = mpsc::channel();
    std::thread::spawn(move || {
        let mut s = String::new();
        let _ = so.read_to_string(&mut s);
        let _ = tx_o.send(s);
    });
    let (tx_e, rx_e) = mpsc::channel();
    std::thread::spawn(move || {
        let mut s = String::new();
        let _ = se.read_to_string(&mut s);
        let _ = tx_e.send(s);
    });

    let deadline = std::time::Instant::now() + timeout;
    let status = loop {
        match child.try_wait() {
            Ok(Some(st)) => break st,
            Ok(None) => {
                if std::time::Instant::now() >= deadline {
                    let _ = child.kill();
                    let _ = child.wait();
                    return Err(PriorError::Timeout);
                }
                std::thread::sleep(Duration::from_millis(20));
            }
            Err(e) => return Err(PriorError::Io(e.to_string())),
        }
    };

    let stdout = rx_o.recv_timeout(Duration::from_secs(2)).unwrap_or_default();
    let stderr = rx_e.recv_timeout(Duration::from_secs(2)).unwrap_or_default();

    if status.success() {
        Ok(stdout)
    } else {
        Err(PriorError::Failed { code: status.code(), stderr })
    }
}
```

`app/src-tauri/src/main.rs` 맨 위(`use` 뒤)에 모듈을 선언한다:

```rust
mod prior;
```

- [ ] **Step 5: 테스트가 통과하는 것을 본다**

```bash
cd /Users/zesty/project/casebook/app/src-tauri
cargo test --lib 2>&1 | tail -8
```

Expected:

```
running 4 tests
test prior::tests::정상_출력을_그대로_준다 ... ok
test prior::tests::없는_바이너리는_NotFound_다 ... ok
test prior::tests::종료코드가_0이_아니면_stderr_를_담는다 ... ok
test prior::tests::상한을_넘으면_Timeout_이다 ... ok

test result: ok. 4 passed; 0 failed
```

`cargo test --lib` 가 "no library targets" 로 실패하면 `Cargo.toml` 에 아래를 더한다:

```toml
[lib]
name = "priorcase_app_lib"
path = "src/lib.rs"
crate-type = ["lib"]
```

그리고 `src/lib.rs` 에 `pub mod prior;` 를 넣은 뒤 `main.rs` 의 `mod prior;` 를 `use priorcase_app_lib::prior;` 로 바꾼다.

- [ ] **Step 6: 커밋**

```bash
cd /Users/zesty/project/casebook
git add app/src-tauri app/fixtures
git commit -m "feat(app): prior 실행과 오류 분류 넷

NotFound·Failed·Timeout·Io 를 나눈다. 한 종류로 뭉치면 '설치가 안 됐다' 와
'설정이 깨졌다' 와 '느리다' 가 같은 화면이 되고 사람은 무엇을 고칠지 모른다.

stdout/stderr 를 스레드로 먼저 빨아들인다 — 파이프를 안 비우면 자식이 버퍼가
차서 멈추고, 그러면 타임아웃이 '느린 명령' 이 아니라 우리가 만든 교착이 된다.

가짜 prior 스크립트 4종으로 각 갈래를 재현한다."
```

---

## Task 3: IPC 커맨드 넷

**Files:**
- Create: `app/src-tauri/src/commands.rs`
- Modify: `app/src-tauri/src/main.rs` (커맨드 등록)

**Interfaces:**
- Consumes: `prior::run(bin, args, timeout) -> Result<String, PriorError>` (Task 2)
- Produces: 프런트에서 부르는 커맨드 넷
  - `queue() -> Result<String, CmdError>` — stdout(JSON 문자열)을 그대로 준다
  - `resolve_pending(id: String) -> Result<(), CmdError>`
  - `promote(id: String) -> Result<String, CmdError>`
  - `review(stem: String, outcome: String) -> Result<(), CmdError>`
  - `CmdError { kind: String, message: String }` — `kind` 는 `not_found|failed|timeout|io`

- [x] **Step 1: 실패하는 테스트를 쓴다**

`app/src-tauri/src/commands.rs`:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    fn fixture(name: &str) -> String {
        let mut p = std::path::PathBuf::from(env!("CARGO_MANIFEST_DIR"));
        p.pop();
        p.push("fixtures");
        p.push(name);
        p.to_string_lossy().into_owned()
    }

    #[test]
    fn 오류_종류가_문자열로_나간다() {
        // 프런트가 종류별로 다른 화면을 그리려면 kind 가 필요하다.
        // Display 문자열만 주면 프런트가 한국어 문구를 파싱하게 되고,
        // 문구를 고치는 순간 화면이 조용히 깨진다.
        let e = to_cmd_error(crate::prior::PriorError::NotFound);
        assert_eq!(e.kind, "not_found");
        let e = to_cmd_error(crate::prior::PriorError::Timeout);
        assert_eq!(e.kind, "timeout");
        let e = to_cmd_error(crate::prior::PriorError::Failed {
            code: Some(1),
            stderr: "설정을 읽을 수 없다".into(),
        });
        assert_eq!(e.kind, "failed");
        assert!(e.message.contains("설정을 읽을 수 없다"), "message={}", e.message);
    }

    #[test]
    fn queue_는_stdout_을_그대로_준다() {
        let out = run_queue(&fixture("fake-prior-ok.sh")).expect("성공해야 한다");
        assert!(out.contains("\"confirm\""), "out={out}");
    }

    #[test]
    fn 쓰기_명령은_긴_상한을_쓴다() {
        // prior promote 는 판별기를 부르고 그 예산이 90초다.
        // 읽기 상한(10초)을 쓰면 정상 승격이 타임아웃으로 보인다.
        assert!(WRITE_TIMEOUT.as_secs() >= 90, "쓰기 상한={:?}", WRITE_TIMEOUT);
        assert!(READ_TIMEOUT.as_secs() <= 15, "읽기 상한={:?}", READ_TIMEOUT);
    }
}
```

- [x] **Step 2: 컴파일이 실패하는 것을 본다**

```bash
cd /Users/zesty/project/casebook/app/src-tauri
cargo test --lib 2>&1 | tail -5
```

Expected: `cannot find function 'to_cmd_error'`, `cannot find value 'WRITE_TIMEOUT'` 등

- [x] **Step 3: 최소 구현을 쓴다**

`app/src-tauri/src/commands.rs` 의 테스트 위에 넣는다:

```rust
use crate::prior::{run, PriorError};
use serde::Serialize;
use std::time::Duration;

/// READ_TIMEOUT 은 `prior queue` 의 상한이다.
///
/// 실측 1.0초(결정 98건 · 볼트 하나)다. 볼트가 늘면 곱해지므로 여유를 준다.
pub const READ_TIMEOUT: Duration = Duration::from_secs(10);

/// WRITE_TIMEOUT 은 쓰기 명령의 상한이다.
///
/// **`prior promote` 가 판별기를 부르고 그 예산이 90초다.** 읽기 상한을 쓰면
/// 정상 승격이 타임아웃으로 보이고, 사람은 눌렀는데 아무 일도 안 난 줄 안다.
pub const WRITE_TIMEOUT: Duration = Duration::from_secs(120);

/// CmdError 는 프런트가 종류별로 다른 화면을 그리기 위한 것이다.
///
/// **kind 를 문자열로 내보내는 이유**: 프런트가 한국어 문구를 파싱하게 하면
/// 문구를 고치는 순간 화면이 조용히 깨진다.
#[derive(Debug, Serialize)]
pub struct CmdError {
    pub kind: String,
    pub message: String,
}

pub fn to_cmd_error(e: PriorError) -> CmdError {
    let kind = match &e {
        PriorError::NotFound => "not_found",
        PriorError::Failed { .. } => "failed",
        PriorError::Timeout => "timeout",
        PriorError::Io(_) => "io",
    };
    CmdError { kind: kind.to_string(), message: e.to_string() }
}

/// prior_bin 은 부를 바이너리 경로다.
///
/// 환경변수로 덮을 수 있게 두는 이유는 테스트다 — 가짜 스크립트로 바꿔 각 오류
/// 상황을 재현한다. 기본은 PATH 의 prior 이고, 없으면 ~/.local/bin/prior 다.
pub fn prior_bin() -> String {
    if let Ok(p) = std::env::var("PRIORCASE_APP_BIN") {
        return p;
    }
    "prior".to_string()
}

pub fn run_queue(bin: &str) -> Result<String, CmdError> {
    run(bin, &["queue", "--json"], READ_TIMEOUT).map_err(to_cmd_error)
}

#[tauri::command]
pub fn queue() -> Result<String, CmdError> {
    run_queue(&prior_bin())
}

#[tauri::command]
pub fn resolve_pending(id: String) -> Result<(), CmdError> {
    run(&prior_bin(), &["pending", "--resolve", &id], WRITE_TIMEOUT)
        .map(|_| ())
        .map_err(to_cmd_error)
}

#[tauri::command]
pub fn promote(id: String) -> Result<String, CmdError> {
    run(&prior_bin(), &["promote", &id, "--json"], WRITE_TIMEOUT).map_err(to_cmd_error)
}

#[tauri::command]
pub fn review(stem: String, outcome: String) -> Result<(), CmdError> {
    run(&prior_bin(), &["review", &stem, "--outcome", &outcome], WRITE_TIMEOUT)
        .map(|_| ())
        .map_err(to_cmd_error)
}
```

`app/src-tauri/src/main.rs` 의 `mod prior;` 아래에 더하고, `Builder` 에 등록한다:

```rust
mod commands;
```

`tauri::Builder::default()` 바로 뒤에 체이닝한다:

```rust
        .invoke_handler(tauri::generate_handler![
            commands::queue,
            commands::resolve_pending,
            commands::promote,
            commands::review,
        ])
```

- [x] **Step 4: 테스트가 통과하는 것을 본다**

```bash
cd /Users/zesty/project/casebook/app/src-tauri
cargo test --lib 2>&1 | tail -10
```

Expected: 7 passed (Task 2 의 4개 + 이번 3개)

- [x] **Step 5: 커밋**

```bash
cd /Users/zesty/project/casebook
git add app/src-tauri
git commit -m "feat(app): IPC 커맨드 넷 (queue·resolve·promote·review)

오류를 kind 문자열로 내보낸다 — 프런트가 한국어 문구를 파싱하게 하면 문구를
고치는 순간 화면이 조용히 깨진다.

쓰기 상한이 읽기 상한과 다르다. prior promote 는 판별기를 부르고 그 예산이
90초라, 읽기 상한(10초)을 쓰면 정상 승격이 타임아웃으로 보인다."
```

---

## Task 4: 계약 타입과 포맷 함수

**Files:**
- Create: `app/src/types.ts`
- Create: `app/src/format.ts`
- Create: `app/tests/format.test.ts`
- Create: `app/vitest.config.ts`

**Interfaces:**
- Produces:
  - `types.ts`: `Queue`, `Confirm`, `Review`, `Retro`, `Health`, `Similar`, `Level`
  - `format.ts`: `vaultLabel(domain: string, vault: string): string`,
    `clip(text: string, lines: number): { shown: string; hidden: number }`,
    `reasonLabel(reason: string, hits: number): string`

- [x] **Step 1: 계약 타입을 쓴다**

`app/src/types.ts`:

```ts
// prior queue --json 의 계약이다.
//
// **손으로 쓴다. 생성하지 않는다.** 계약이 바뀌면 여기가 컴파일 에러로 깨져야
// 하고, 그 순간이 "앱도 같이 고쳐야 한다" 를 알아채는 자리다. Go 쪽의
// queue_test.go 가 같은 목록을 못 박는다.

export type Level = "ok" | "warn" | "fail" | "unknown";

export interface Similar {
  stem: string;
  path: string;
  summary: string;
  /** 회수 점수. **절대값으로 판정하면 안 된다** — 진짜 일치 65점과 가짜 1위
   * 54점이 겹친 실측이 있다. 같은 목록 안의 상대 비교로만 쓸모가 있다. */
  score: number;
}

export interface Confirm {
  id: string;
  domain: string;
  /** 빈 문자열이면 "볼트를 모른다"(설정 오류)다. 기본 볼트로 그리면 안 된다. */
  vault: string;
  when: string;
  signals: string[];
  excerpt: string;
  fails: number;
  /** true 면 자동으로는 다시 안 온다 — 기다리라고 그리면 안 된다. */
  gave_up: boolean;
  similar: Similar[];
}

export interface Review {
  id: string;
  domain: string;
  vault: string;
  at: string;
  path: string;
  /** 빈 문자열일 수 있다 (2026-08-12 이전 원장). 그때는 "대조할 발췌가 없다"
   * 고 말해야 한다 — 조용히 안 보여 주면 사람은 노트만 보고 맞다고 누른다. */
  excerpt: string;
}

export interface Retro {
  stem: string;
  date: string;
  domain: string;
  vault: string;
  summary: string;
  author?: string;
  reason: "recalled" | "superseded";
  hits: number;
}

export interface Health {
  name: string;
  level: Level;
  detail: string;
  fix?: string;
}

export interface Queue {
  confirm: Confirm[];
  review: Review[];
  retro: Retro[];
  health: Health[];
  /** 없으면 키 자체가 빠진다. 있으면 **큐가 불완전하다**는 뜻이다. */
  warnings?: string[];
}

/** IPC 오류. kind 로 화면을 가른다. */
export interface CmdError {
  kind: "not_found" | "failed" | "timeout" | "io";
  message: string;
}
```

- [x] **Step 2: 실패하는 테스트를 쓴다**

`app/vitest.config.ts`:

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: { environment: "jsdom", include: ["tests/**/*.test.ts"] },
});
```

`app/tests/format.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { vaultLabel, clip, reasonLabel } from "../src/format";

describe("vaultLabel", () => {
  it("도메인과 볼트를 같이 보여 준다", () => {
    expect(vaultLabel("priorcase", "personal")).toBe("priorcase · personal 볼트");
  });

  // ★ 볼트가 비면 설정 오류다. 기본 볼트로 그리면 사람은 거기 쌓이는 줄 알지만
  //   실제로는 기록 자체가 실패한다 (capture 가 없는 볼트를 거부한다).
  it("볼트를 모르면 그렇다고 말한다", () => {
    expect(vaultLabel("priorcase", "")).toBe("priorcase · ⚠️ 볼트 미상");
  });
});

describe("clip", () => {
  it("줄 수를 넘으면 접고 몇 줄이 남았는지 준다", () => {
    const text = ["a", "b", "c", "d", "e"].join("\n");
    expect(clip(text, 3)).toEqual({ shown: "a\nb\nc", hidden: 2 });
  });

  it("짧으면 그대로 준다", () => {
    expect(clip("a\nb", 3)).toEqual({ shown: "a\nb", hidden: 0 });
  });

  // ★ 발췌가 실측 880B~4.9KB 이고 절반이 한 화면에 안 들어간다.
  //   접는 것 자체보다 "몇 줄이 숨었나" 를 아는 것이 중요하다.
  it("빈 문자열도 터지지 않는다", () => {
    expect(clip("", 3)).toEqual({ shown: "", hidden: 0 });
  });
});

describe("reasonLabel", () => {
  it("재회수는 횟수를 보여 준다", () => {
    expect(reasonLabel("recalled", 4)).toBe("재회수 4회");
  });

  // ★ superseded 는 hits 가 0 일 수 있다 — "재회수 0회" 로 그리면 거짓이다.
  it("뒤집힌 것은 횟수를 말하지 않는다", () => {
    expect(reasonLabel("superseded", 0)).toBe("뒤집혔다");
    expect(reasonLabel("superseded", 3)).toBe("뒤집혔다");
  });
});
```

- [x] **Step 3: 테스트가 실패하는 것을 본다**

```bash
cd /Users/zesty/project/casebook/app
pnpm test 2>&1 | tail -8
```

Expected: `Failed to resolve import "../src/format"`

- [x] **Step 4: 최소 구현을 쓴다**

`app/src/format.ts`:

```ts
/** vaultLabel 은 "어느 프로젝트의, 어느 볼트에" 를 한 줄로 만든다.
 *
 * 볼트가 비면 **설정 오류**다 — 도메인이 설정에 없는 볼트를 가리킨다는 뜻이고,
 * 그 경우 기록 자체가 실패한다(capture 가 거부한다). 기본 볼트로 그리면 사람은
 * 거기 쌓이는 줄 알고, 보여 준 것과 실제가 어긋난다. */
export function vaultLabel(domain: string, vault: string): string {
  return vault ? `${domain} · ${vault} 볼트` : `${domain} · ⚠️ 볼트 미상`;
}

/** clip 은 긴 글을 줄 단위로 접고 **몇 줄이 숨었는지** 함께 준다.
 *
 * 발췌가 실측 880B~4.9KB 이고 절반이 한 화면에 안 들어간다. 숨은 줄 수를 안
 * 보여 주면 사람은 그게 전부인 줄 안다. */
export function clip(text: string, lines: number): { shown: string; hidden: number } {
  const all = text.split("\n");
  if (all.length <= lines) return { shown: text, hidden: 0 };
  return { shown: all.slice(0, lines).join("\n"), hidden: all.length - lines };
}

/** reasonLabel 은 회고 방아쇠를 사람 말로 바꾼다.
 *
 * superseded 는 hits 가 0 일 수 있다 — 뒤집혔다는 사실만으로 올라오기 때문이다.
 * 그때 "재회수 0회" 로 그리면 거짓이다. */
export function reasonLabel(reason: string, hits: number): string {
  return reason === "superseded" ? "뒤집혔다" : `재회수 ${hits}회`;
}
```

- [x] **Step 5: 테스트가 통과하는 것을 본다**

```bash
cd /Users/zesty/project/casebook/app
pnpm add -D jsdom
pnpm test 2>&1 | tail -8
```

Expected: `Test Files  1 passed (1)` · `Tests  7 passed (7)`

- [x] **Step 6: 커밋**

```bash
cd /Users/zesty/project/casebook
git add app/src/types.ts app/src/format.ts app/tests app/vitest.config.ts app/package.json app/pnpm-lock.yaml
git commit -m "feat(app): 계약 타입과 포맷 함수

types.ts 는 손으로 쓴다. 생성하면 계약이 바뀌어도 컴파일이 통과하고, 그러면
'앱도 같이 고쳐야 한다' 를 알아챌 자리가 없다.

vault 가 비면 '볼트 미상' 으로 그린다 — 설정 오류이고, 기본 볼트로 그리면
보여 준 것과 실제로 기록되는 자리가 어긋난다.

superseded 는 hits 가 0 일 수 있어 '재회수 0회' 로 그리면 거짓이다."
```

---

## Task 5: 셸 — 탭·배너·빈 상태·오류 화면

**Files:**
- Create: `app/src/api.ts`
- Create: `app/src/render/shell.ts`
- Create: `app/tests/shell.test.ts`
- Modify: `app/src/style.css`

**Interfaces:**
- Consumes: `types.ts` (Task 4), IPC 커맨드 (Task 3)
- Produces:
  - `api.ts`: `fetchQueue(): Promise<Queue>`, `resolvePending(id)`, `promote(id)`, `review(stem, outcome)` — 전부 `CmdError` 를 throw
  - `shell.ts`: `renderError(el: HTMLElement, e: CmdError): void`,
    `renderWarnings(el: HTMLElement, warnings: string[] | undefined): void`,
    `renderEmpty(el: HTMLElement, what: string): void`

- [x] **Step 1: API 래퍼를 쓴다**

`app/src/api.ts`:

```ts
import { invoke } from "@tauri-apps/api/core";
import type { Queue, CmdError } from "./types";

/** **여기만 백엔드를 안다.** 화면 코드가 invoke 를 직접 부르면 가짜로 바꿔
 * 테스트하기가 어려워지고, 커맨드 이름이 화면마다 흩어진다. */

function asCmdError(e: unknown): CmdError {
  if (e && typeof e === "object" && "kind" in e && "message" in e) {
    return e as CmdError;
  }
  return { kind: "io", message: String(e) };
}

export async function fetchQueue(): Promise<Queue> {
  let raw: string;
  try {
    raw = await invoke<string>("queue");
  } catch (e) {
    throw asCmdError(e);
  }
  try {
    return JSON.parse(raw) as Queue;
  } catch {
    // **깨진 JSON 을 빈 큐로 그리면 안 된다.** 그러면 고장이 "할 일 없음" 이 된다.
    throw { kind: "failed", message: `prior 가 JSON 이 아닌 것을 냈다: ${raw.slice(0, 200)}` } as CmdError;
  }
}

export async function resolvePending(id: string): Promise<void> {
  try {
    await invoke("resolve_pending", { id });
  } catch (e) {
    throw asCmdError(e);
  }
}

export async function promote(id: string): Promise<void> {
  try {
    await invoke<string>("promote", { id });
  } catch (e) {
    throw asCmdError(e);
  }
}

export async function review(stem: string, outcome: "good" | "bad"): Promise<void> {
  try {
    await invoke("review", { stem, outcome });
  } catch (e) {
    throw asCmdError(e);
  }
}
```

- [x] **Step 2: 실패하는 테스트를 쓴다**

`app/tests/shell.test.ts`:

```ts
import { describe, it, expect, beforeEach } from "vitest";
import { renderError, renderWarnings, renderEmpty } from "../src/render/shell";

let el: HTMLElement;
beforeEach(() => {
  el = document.createElement("div");
});

describe("renderError", () => {
  // ★★ 오류 종류마다 사람이 할 일이 다르다. 한 화면으로 뭉치면 안 된다.
  it("설치가 안 됐으면 설치 안내를 낸다", () => {
    renderError(el, { kind: "not_found", message: "prior 를 찾을 수 없다" });
    expect(el.textContent).toContain("설치");
    expect(el.textContent).not.toContain("할 일이 없");
  });

  it("실패는 stderr 를 그대로 보여 준다", () => {
    renderError(el, { kind: "failed", message: "설정을 읽을 수 없다 (/x/config.toml)" });
    expect(el.textContent).toContain("/x/config.toml");
  });

  it("타임아웃은 느리다고 말한다", () => {
    renderError(el, { kind: "timeout", message: "상한 안에 끝나지 않았다" });
    expect(el.textContent).toContain("오래");
  });
});

describe("renderWarnings", () => {
  // ★★ warnings 가 있으면 큐가 **불완전**하다. "할 일 없음" 으로 그리면 안 된다.
  it("경고가 있으면 배너를 낸다", () => {
    renderWarnings(el, ["볼트 work 에 접근할 수 없다"]);
    expect(el.textContent).toContain("볼트 work");
    expect(el.textContent).toContain("불완전");
  });

  it("경고가 없으면 아무것도 안 그린다", () => {
    renderWarnings(el, undefined);
    expect(el.innerHTML).toBe("");
    renderWarnings(el, []);
    expect(el.innerHTML).toBe("");
  });
});

describe("renderEmpty", () => {
  // ★ 빈 상태가 "고장난 것처럼" 보이면 안 된다.
  it("무엇이 비었는지 말한다", () => {
    renderEmpty(el, "확인할 구간");
    expect(el.textContent).toContain("확인할 구간");
    expect(el.textContent).toContain("없");
  });
});
```

- [x] **Step 3: 테스트가 실패하는 것을 본다**

```bash
cd /Users/zesty/project/casebook/app
pnpm test 2>&1 | tail -6
```

Expected: `Failed to resolve import "../src/render/shell"`

- [x] **Step 4: 최소 구현을 쓴다**

`app/src/render/shell.ts`:

```ts
import type { CmdError } from "../types";

function el(tag: string, cls: string, text?: string): HTMLElement {
  const n = document.createElement(tag);
  n.className = cls;
  if (text !== undefined) n.textContent = text;
  return n;
}

/** renderError 는 **오류 종류마다 다른 화면**을 그린다.
 *
 * 한 화면으로 뭉치면 "설치가 안 됐다" 와 "설정이 깨졌다" 와 "느리다" 가 같아
 * 보이고, 사람은 무엇을 고쳐야 할지 모른다. */
export function renderError(root: HTMLElement, e: CmdError): void {
  root.replaceChildren();
  const box = el("div", "error");
  switch (e.kind) {
    case "not_found":
      box.append(
        el("h2", "error-title", "prior 가 설치돼 있지 않다"),
        el("p", "error-body",
          "터미널에서 `go build -o prior ./cmd/prior && install -m 0755 ./prior ~/.local/bin/prior` " +
          "를 실행하거나, PRIORCASE_APP_BIN 으로 경로를 지정하라."),
      );
      break;
    case "timeout":
      box.append(
        el("h2", "error-title", "prior 가 너무 오래 걸린다"),
        el("p", "error-body",
          "볼트가 크면 큐 계산이 느려질 수 있다. `prior queue --json` 을 직접 돌려 " +
          "얼마나 걸리는지 재 보라."),
      );
      break;
    default:
      box.append(
        el("h2", "error-title", "prior 가 실패했다"),
        // stderr 를 그대로 보여 준다 — 사람이 고칠 유일한 단서다.
        el("pre", "error-stderr", e.message),
      );
  }
  root.append(box);
}

/** renderWarnings 는 큐가 불완전하다는 사실을 위에 띄운다.
 *
 * **비어 있으면 아무것도 안 그린다.** 늘 배너가 있으면 사람이 그것을 무시하는
 * 법을 배우고, 그러면 진짜 경고가 안 보인다. */
export function renderWarnings(root: HTMLElement, warnings: string[] | undefined): void {
  root.replaceChildren();
  if (!warnings || warnings.length === 0) return;
  const box = el("div", "warnings");
  box.append(el("strong", "warnings-title", "⚠️ 큐가 불완전하다"));
  const ul = el("ul", "warnings-list");
  for (const w of warnings) ul.append(el("li", "warnings-item", w));
  box.append(ul);
  root.append(box);
}

/** renderEmpty 는 빈 상태를 **고장이 아닌 것처럼** 그린다. */
export function renderEmpty(root: HTMLElement, what: string): void {
  root.replaceChildren(el("p", "empty", `${what}이 없다.`));
}
```

- [x] **Step 5: 스타일을 더한다**

`app/src/style.css` 맨 아래에 붙인다:

```css
.error { padding: 16px; }
.error-title { font-size: 14px; margin: 0 0 8px; }
.error-body { color: #666; line-height: 1.5; margin: 0; }
.error-stderr { background: #0001; padding: 8px; border-radius: 4px; white-space: pre-wrap; font-size: 11px; margin: 0; }
.warnings { background: #fbbf2422; border: 1px solid #fbbf2455; border-radius: 6px; padding: 8px 10px; margin-bottom: 10px; }
.warnings-list { margin: 4px 0 0; padding-left: 18px; }
.warnings-item { color: #92400e; }
.empty { color: #888; text-align: center; padding: 28px 0; margin: 0; }
```

- [x] **Step 6: 테스트가 통과하는 것을 본다**

```bash
cd /Users/zesty/project/casebook/app
pnpm test 2>&1 | tail -6
```

Expected: `Tests  13 passed (13)` (Task 4 의 7개 + 이번 6개)

- [x] **Step 7: 커밋**

```bash
cd /Users/zesty/project/casebook
git add app/src/api.ts app/src/render app/tests/shell.test.ts app/src/style.css
git commit -m "feat(app): 셸 — 오류 종류별 화면 · 경고 배너 · 빈 상태

깨진 JSON 을 빈 큐로 그리지 않는다. 그러면 고장이 '할 일 없음' 이 되고, 이
시스템은 모든 부품이 실패해도 대화를 막지 않도록 설계돼 있어서 앱마저 그러면
고장이 영영 안 보인다.

경고가 없으면 배너를 아예 안 그린다 — 늘 있는 경고는 무시하는 법을 가르친다.

api.ts 만 백엔드를 안다."
```

---

## Task 6: 확인 큐 화면

**Files:**
- Create: `app/src/render/confirm.ts`
- Create: `app/tests/confirm.test.ts`
- Modify: `app/src/style.css`

**Interfaces:**
- Consumes: `types.ts`, `format.ts` (Task 4), `shell.ts` (Task 5)
- Produces: `renderConfirm(root: HTMLElement, items: Confirm[], on: ConfirmActions): void`
  where `interface ConfirmActions { resolve(id: string): void; promote(id: string): void }`

- [x] **Step 1: 실패하는 테스트를 쓴다**

`app/tests/confirm.test.ts`:

```ts
import { describe, it, expect, beforeEach, vi } from "vitest";
import { renderConfirm } from "../src/render/confirm";
import type { Confirm } from "../src/types";

const base: Confirm = {
  id: "/t.jsonl@0",
  domain: "priorcase",
  vault: "personal",
  when: "2026-08-11",
  signals: ["결정", "선택"],
  excerpt: "에이전트: 저장 엔진을 SQLite 로 정했다.",
  fails: 0,
  gave_up: false,
  similar: [],
};

let root: HTMLElement;
const actions = { resolve: vi.fn(), promote: vi.fn() };
beforeEach(() => {
  root = document.createElement("div");
  actions.resolve.mockClear();
  actions.promote.mockClear();
});

describe("renderConfirm", () => {
  it("비었으면 빈 상태를 그린다", () => {
    renderConfirm(root, [], actions);
    expect(root.textContent).toContain("확인할 구간");
  });

  it("도메인과 볼트를 같이 보여 준다", () => {
    renderConfirm(root, [base], actions);
    expect(root.textContent).toContain("priorcase · personal 볼트");
  });

  // ★★ 포기한 줄은 자동으로 다시 안 온다. 기다리라고 그리면 사람은 영영 기다린다.
  it("포기한 줄은 그렇다고 말한다", () => {
    renderConfirm(root, [{ ...base, fails: 3, gave_up: true }], actions);
    expect(root.textContent).toContain("자동 처리 포기");
    expect(root.textContent).toContain("3");
  });

  it("실패했지만 아직 포기 전이면 횟수만 보여 준다", () => {
    renderConfirm(root, [{ ...base, fails: 2, gave_up: false }], actions);
    expect(root.textContent).toContain("2");
    expect(root.textContent).not.toContain("포기");
  });

  // ★★ 비슷한 결정은 **점수와 함께** 보여 주되 단정하면 안 된다.
  //    회수는 언제나 무언가를 돌려주므로 일치가 없어도 1위가 나온다.
  it("비슷한 결정을 점수와 함께 보여 준다", () => {
    renderConfirm(root, [{
      ...base,
      similar: [{ stem: "priorcase-결정-저장엔진-2026-08-01", path: "p", summary: "s", score: 118 }],
    }], actions);
    expect(root.textContent).toContain("118");
    expect(root.textContent).toContain("저장엔진");
    // 단정 금지
    expect(root.textContent).not.toContain("이미 기록");
    expect(root.textContent).not.toContain("중복");
  });

  it("시그널이 없어도 터지지 않는다", () => {
    // 판별기가 있으면 시그널 필터를 건너뛴다(D9) — 비는 것이 정상이다.
    renderConfirm(root, [{ ...base, signals: [] }], actions);
    expect(root.textContent).toContain("priorcase");
  });

  it("버튼이 콜백을 부른다", () => {
    renderConfirm(root, [base], actions);
    const btns = root.querySelectorAll("button");
    const 결정 = Array.from(btns).find((b) => b.textContent?.includes("결정이다"))!;
    const 아니다 = Array.from(btns).find((b) => b.textContent?.includes("아니다"))!;
    결정.click();
    expect(actions.promote).toHaveBeenCalledWith("/t.jsonl@0");
    아니다.click();
    expect(actions.resolve).toHaveBeenCalledWith("/t.jsonl@0");
  });
});
```

- [x] **Step 2: 테스트가 실패하는 것을 본다**

```bash
cd /Users/zesty/project/casebook/app
pnpm test 2>&1 | tail -6
```

Expected: `Failed to resolve import "../src/render/confirm"`

- [x] **Step 3: 최소 구현을 쓴다**

`app/src/render/confirm.ts`:

```ts
import type { Confirm } from "../types";
import { vaultLabel, clip } from "../format";
import { renderEmpty } from "./shell";

export interface ConfirmActions {
  resolve(id: string): void;
  promote(id: string): void;
}

const EXCERPT_LINES = 8;

function el(tag: string, cls: string, text?: string): HTMLElement {
  const n = document.createElement(tag);
  n.className = cls;
  if (text !== undefined) n.textContent = text;
  return n;
}

export function renderConfirm(root: HTMLElement, items: Confirm[], on: ConfirmActions): void {
  root.replaceChildren();
  if (items.length === 0) {
    renderEmpty(root, "확인할 구간");
    return;
  }
  for (const it of items) {
    const card = el("div", "card");

    // 머리: 언제 · 어느 프로젝트 · 어느 볼트 · 시그널
    const head = el("div", "card-head");
    head.append(el("span", "when", it.when), el("span", "vault", vaultLabel(it.domain, it.vault)));
    if (it.signals.length > 0) {
      head.append(el("span", "signals", `시그널 ${it.signals.join("·")}`));
    }
    // **판별기 실패는 반드시 드러낸다.** 포기한 줄은 자동으로 다시 안 온다 —
    // 그 사실을 안 보여 주면 사람은 곧 처리되겠거니 하고 영영 기다린다.
    if (it.gave_up) {
      head.append(el("span", "gaveup", `⚠️ 자동 처리 포기 (판별기 ${it.fails}회 실패)`));
    } else if (it.fails > 0) {
      head.append(el("span", "fails", `판별기 ${it.fails}회 실패`));
    }
    card.append(head);

    // 비슷한 기존 결정 — **점수와 함께, 단정 없이.**
    if (it.similar.length > 0) {
      const box = el("div", "similar");
      box.append(el("div", "similar-title", "비슷한 기존 결정"));
      for (const s of it.similar) {
        const row = el("div", "similar-row");
        row.append(el("span", "score", String(s.score)), el("span", "stem", s.stem));
        if (s.summary) row.append(el("div", "similar-summary", s.summary));
        box.append(row);
      }
      card.append(box);
    }

    // 발췌 — 접고 몇 줄이 숨었는지 말한다
    const { shown, hidden } = clip(it.excerpt, EXCERPT_LINES);
    const ex = el("pre", "excerpt", shown);
    card.append(ex);
    if (hidden > 0) card.append(el("div", "excerpt-more", `… ${hidden}줄 더`));

    // 버튼 둘
    const btns = el("div", "buttons");
    const yes = el("button", "primary", "결정이다 → 기록한다") as HTMLButtonElement;
    yes.addEventListener("click", () => on.promote(it.id));
    const no = el("button", "secondary", "아니다 → 지운다") as HTMLButtonElement;
    no.addEventListener("click", () => on.resolve(it.id));
    btns.append(yes, no);
    card.append(btns);

    root.append(card);
  }
}
```

- [x] **Step 4: 스타일을 더한다**

`app/src/style.css` 맨 아래에 붙인다:

```css
.card { border: 1px solid #0002; border-radius: 8px; padding: 10px 12px; margin-bottom: 10px; }
.card-head { display: flex; flex-wrap: wrap; gap: 8px; align-items: baseline; font-size: 11px; color: #666; margin-bottom: 6px; }
.card-head .vault { font-weight: 600; color: inherit; }
.gaveup { color: #b45309; font-weight: 600; }
.fails { color: #92400e; }
.similar { background: #0001; border-radius: 6px; padding: 6px 8px; margin-bottom: 6px; font-size: 11px; }
.similar-title { color: #666; margin-bottom: 4px; }
.similar-row { display: flex; gap: 6px; align-items: baseline; }
.similar-row .score { min-width: 32px; text-align: right; color: #666; font-variant-numeric: tabular-nums; }
.similar-summary { color: #666; margin: 0 0 4px 38px; }
.excerpt { white-space: pre-wrap; font-size: 11px; background: #0001; padding: 8px; border-radius: 4px; margin: 0; max-height: 180px; overflow: auto; }
.excerpt-more { font-size: 11px; color: #888; margin-top: 4px; }
.buttons { display: flex; gap: 8px; margin-top: 8px; }
button { font: inherit; padding: 5px 10px; border-radius: 6px; border: 1px solid #0003; cursor: pointer; background: #0001; }
button.primary { background: #2563eb; color: #fff; border-color: transparent; }
```

- [x] **Step 5: 테스트가 통과하는 것을 본다**

```bash
cd /Users/zesty/project/casebook/app
pnpm test 2>&1 | tail -6
```

Expected: `Tests  20 passed (20)`

- [x] **Step 6: 커밋**

```bash
cd /Users/zesty/project/casebook
git add app/src/render/confirm.ts app/tests/confirm.test.ts app/src/style.css
git commit -m "feat(app): 확인 큐 화면

포기한 줄을 반드시 드러낸다 — 자동으로는 다시 안 오는데 그 사실을 안 보여 주면
사람은 곧 처리되겠거니 하고 영영 기다린다.

비슷한 기존 결정을 점수와 함께 보여 주되 단정하지 않는다. 회수는 언제나 무언가를
돌려주므로 일치가 없는 발췌도 1위가 나온다 (실측: 진짜 일치 65점과 가짜 1위
54점이 겹쳤다). '이미 기록됨' 같은 문구를 쓰지 않는다."
```

---

## Task 7: `prior path <stem>` — 노트의 절대 경로를 준다

**Files:**
- Create: `internal/adapter/cli/path.go`
- Create: `internal/adapter/cli/path_test.go`
- Modify: `internal/adapter/cli/root.go` (명령 등록)

**Interfaces:**
- Produces: `prior path <stem>` — 결정 노트의 **절대 경로**를 stdout 에 한 줄로 낸다. 없으면 0 아닌 코드로 끝난다.

**왜 필요한가.** 검토 큐의 `[노트 열기]` 가 파일을 열려면 절대 경로가 필요한데,
`queue --json` 의 `review[].path` 는 **볼트 상대 경로**다. 앱이 볼트 절대 경로를
따로 계산하면 **볼트 선택 규칙이 둘이 된다** — 다중 볼트에서 그 어긋남은 앱이
엉뚱한 파일을 열거나 못 여는 것으로 나타난다.

`prior doctor --json` 은 없다(확인함). 상태 검사의 `detail` 에 볼트 경로가 들어
있지만 그건 **사람이 읽는 문구**라 앱이 파싱하면 문구를 못 고치게 된다.

- [x] **Step 1: 실패하는 테스트를 쓴다**

`internal/adapter/cli/path_test.go`:

```go
package cli

import (
	"path/filepath"
	"strings"
	"testing"

	"github.com/spf13/cobra"
	"github.com/xian0310567/priorcase/internal/testutil"
)

// ★★ 앱이 노트를 열려면 절대 경로가 필요하다.
//
// queue --json 의 review[].path 는 볼트 상대 경로다. 앱이 볼트 절대 경로를
// 따로 계산하면 **볼트 선택 규칙이 둘이 된다** — 다중 볼트에서 그 어긋남은
// 앱이 엉뚱한 파일을 열거나 못 여는 것으로 나타난다.
func TestPathCmdPrintsAbsolutePath(t *testing.T) {
	cfgPath, c := testutil.VaultConfigFile(t)

	out, err := runPath(t, cfgPath, "alpha-결정-저장엔진-2026-08-01")
	if err != nil {
		t.Fatalf("실행 실패: %v", err)
	}
	got := strings.TrimSpace(out)
	if !filepath.IsAbs(got) {
		t.Errorf("절대 경로가 아니다: %q", got)
	}
	if !strings.HasPrefix(got, c.DefaultVaultPath()) {
		t.Errorf("볼트 밖을 가리킨다: %q (볼트 %q)", got, c.DefaultVaultPath())
	}
	if !strings.HasSuffix(got, ".md") {
		t.Errorf(".md 가 아니다: %q", got)
	}
}

// ★ 없는 노트는 시끄럽게 실패해야 한다. 빈 줄을 주면 앱이 빈 경로를 연다.
func TestPathCmdFailsOnUnknownStem(t *testing.T) {
	cfgPath, _ := testutil.VaultConfigFile(t)
	if _, err := runPath(t, cfgPath, "없는-결정-x-2026-01-01"); err == nil {
		t.Fatal("없는 stem 인데 통과했다")
	}
}

func runPath(t *testing.T, cfgPath, stem string) (string, error) {
	t.Helper()
	cmd := newPathCmd()
	root := &cobra.Command{Use: "prior"}
	root.PersistentFlags().String("config", "", "")
	root.AddCommand(cmd)
	var out, errb strings.Builder
	root.SetOut(&out)
	root.SetErr(&errb)
	root.SetArgs([]string{"path", stem, "--config", cfgPath})
	err := root.Execute()
	return out.String(), err
}
```

- [x] **Step 2: 테스트가 실패하는 것을 본다**

```bash
cd /Users/zesty/project/casebook
export GOTOOLCHAIN=auto
go test ./internal/adapter/cli/ -run TestPathCmd -count=1 2>&1 | tail -5
```

Expected: `undefined: newPathCmd`

- [x] **Step 3: 최소 구현을 쓴다**

`internal/adapter/cli/path.go`:

```go
package cli

import (
	"fmt"

	"github.com/spf13/cobra"
)

// newPathCmd 는 `prior path <stem>` 이다.
//
// **경로 해석을 CLI 안에 둔다.** 앱이 볼트 경로를 조립하기 시작하면 볼트 선택
// 규칙이 둘이 되고, 다중 볼트에서 그 어긋남은 앱이 엉뚱한 파일을 열거나 못
// 여는 것으로 나타난다. 앱은 이 출력을 그대로 OS 에 넘긴다.
func newPathCmd() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "path <stem>",
		Short: "결정 노트의 절대 경로를 낸다",
		Long: "결정 노트 stem 을 받아 절대 경로를 한 줄로 낸다.\n\n" +
			"데스크탑 앱이 노트를 열 때 쓴다 — 앱이 볼트 경로를 조립하면\n" +
			"볼트 선택 규칙이 둘이 된다.",
		Args:          cobra.ExactArgs(1),
		SilenceUsage:  true,
		SilenceErrors: true,
		RunE: func(cmd *cobra.Command, args []string) error {
			c, l, err := loadFrom(cmd)
			if err != nil {
				return err
			}
			_ = c
			// **볼트를 stem 의 도메인에서 고른다.** cwd 의 볼트로 풀면 다른
			// 프로젝트의 노트를 못 연다.
			ll, err := l.For(l.PrefixOf(args[0]))
			if err != nil {
				return err
			}
			p, err := ll.ResolveStem(args[0])
			if err != nil {
				return err
			}
			fmt.Fprintln(cmd.OutOrStdout(), p)
			return nil
		},
	}
	return cmd
}
```

`internal/adapter/cli/root.go` 에서 다른 `AddCommand` 옆에 더한다:

```go
	root.AddCommand(newPathCmd())
```

- [x] **Step 4: 테스트가 통과하는 것을 본다**

```bash
cd /Users/zesty/project/casebook
export GOTOOLCHAIN=auto
gofmt -w internal/adapter/cli/
go test ./internal/adapter/cli/ -run TestPathCmd -count=1 -v 2>&1 | grep -E '^--- |^ok|FAIL'
```

Expected: 두 테스트 PASS

- [x] **Step 5: 전체 테스트와 실기기 확인**

```bash
cd /Users/zesty/project/casebook
go test ./... -count=1 -timeout 300s 2>&1 | grep -c '^ok'
go build -o prior ./cmd/prior && install -m 0755 ./prior ~/.local/bin/prior
prior path priorcase-결정-다중볼트-프로젝트가볼트를고른다-2026-08-13
```

Expected: 21, 그리고 볼트 아래 절대 경로 한 줄

- [x] **Step 6: 커밋**

```bash
cd /Users/zesty/project/casebook
git add internal/adapter/cli
git commit -m "feat(cli): prior path <stem> — 노트의 절대 경로

데스크탑 앱의 [노트 열기] 가 쓴다. queue --json 의 review[].path 는 볼트 상대
경로이고, 앱이 볼트 절대 경로를 따로 계산하면 볼트 선택 규칙이 둘이 된다 —
다중 볼트에서 그 어긋남은 앱이 엉뚱한 파일을 열거나 못 여는 것으로 나타난다.

볼트는 stem 의 도메인에서 고른다. cwd 의 볼트로 풀면 다른 프로젝트의 노트를
못 연다."
```

---

## Task 8: 검토 큐 화면

**Files:**
- Create: `app/src/render/review.ts`
- Create: `app/tests/review.test.ts`
- Modify: `app/src/style.css`

**Interfaces:**
- Consumes: `types.ts`, `format.ts`, `shell.ts`
- Produces: `renderReview(root: HTMLElement, items: Review[], on: ReviewActions): void`
  where `interface ReviewActions { ok(stem: string): void; open(path: string): void }`

  `stem` 은 `path` 에서 유도한다 — `review` 명령이 stem 을 받기 때문이다
  (`prior review <stem>`). `path` 는 볼트 상대 경로(`priorcase/decisions/x.md`)이므로
  basename 에서 `.md` 를 떼면 stem 이다.

- [x] **Step 1: 실패하는 테스트를 쓴다**

`app/tests/review.test.ts`:

```ts
import { describe, it, expect, beforeEach, vi } from "vitest";
import { renderReview, stemOf } from "../src/render/review";
import type { Review } from "../src/types";

const base: Review = {
  id: "/t.jsonl@0",
  domain: "priorcase",
  vault: "personal",
  at: "2026-08-12T10:00:00Z",
  path: "priorcase/decisions/priorcase-결정-저장엔진-2026-08-12.md",
  excerpt: "사용자: SQLite 로 가자\n에이전트: 확인했습니다",
};

let root: HTMLElement;
const actions = { ok: vi.fn(), open: vi.fn() };
beforeEach(() => {
  root = document.createElement("div");
  actions.ok.mockClear();
  actions.open.mockClear();
});

describe("stemOf", () => {
  it("볼트 상대 경로에서 stem 을 뽑는다", () => {
    expect(stemOf("priorcase/decisions/priorcase-결정-저장엔진-2026-08-12.md"))
      .toBe("priorcase-결정-저장엔진-2026-08-12");
  });
});

describe("renderReview", () => {
  it("비었으면 빈 상태를 그린다", () => {
    renderReview(root, [], actions);
    expect(root.textContent).toContain("검토할 노트");
  });

  it("발췌와 노트 경로를 나란히 놓는다", () => {
    renderReview(root, [base], actions);
    expect(root.textContent).toContain("SQLite 로 가자");
    expect(root.textContent).toContain("priorcase-결정-저장엔진-2026-08-12");
  });

  // ★★★ 발췌가 없으면 **없다고 말해야 한다.**
  //     조용히 안 보여 주면 사람은 노트만 보고 맞다고 누른다 — 그게 이 화면의
  //     존재 이유(판별기 날조 검증)를 통째로 무너뜨린다.
  it("대조할 발췌가 없으면 그렇다고 말한다", () => {
    renderReview(root, [{ ...base, excerpt: "" }], actions);
    expect(root.textContent).toContain("대조할 발췌가 없다");
  });

  it("맞다 버튼이 stem 으로 콜백을 부른다", () => {
    renderReview(root, [base], actions);
    const btn = Array.from(root.querySelectorAll("button"))
      .find((b) => b.textContent?.includes("맞다"))!;
    btn.click();
    expect(actions.ok).toHaveBeenCalledWith("priorcase-결정-저장엔진-2026-08-12");
  });

  // ★ 앱이 볼트 경로를 조립하지 않으려면 stem 을 넘겨야 한다 —
  //   prior path <stem> 이 경로를 푼다.
  it("노트 열기 버튼이 stem 으로 콜백을 부른다", () => {
    renderReview(root, [base], actions);
    const btn = Array.from(root.querySelectorAll("button"))
      .find((b) => b.textContent?.includes("노트 열기"))!;
    btn.click();
    expect(actions.open).toHaveBeenCalledWith("priorcase-결정-저장엔진-2026-08-12");
  });
});
```

- [x] **Step 2: 테스트가 실패하는 것을 본다**

```bash
cd /Users/zesty/project/casebook/app
pnpm test 2>&1 | tail -6
```

Expected: `Failed to resolve import "../src/render/review"`

- [x] **Step 3: 최소 구현을 쓴다**

`app/src/render/review.ts`:

```ts
import type { Review } from "../types";
import { vaultLabel, clip } from "../format";
import { renderEmpty } from "./shell";

export interface ReviewActions {
  /** 노트가 맞다 → prior review <stem> --outcome good */
  ok(stem: string): void;
  /** 노트를 연다 (고치거나 지우는 것은 사람이 에디터에서 한다).
   *
   * **stem 을 넘긴다.** 앱이 볼트 경로를 조립하지 않으려면 `prior path <stem>`
   * 에게 물어야 하고, 그 명령이 stem 을 받는다. */
  open(stem: string): void;
}

const EXCERPT_LINES = 12;

/** stemOf 는 볼트 상대 경로에서 결정 노트의 stem 을 뽑는다.
 *
 * `prior review` 가 stem 을 받으므로 필요하다 (`prior review <stem>`). */
export function stemOf(path: string): string {
  const base = path.split("/").pop() ?? path;
  return base.endsWith(".md") ? base.slice(0, -3) : base;
}

function el(tag: string, cls: string, text?: string): HTMLElement {
  const n = document.createElement(tag);
  n.className = cls;
  if (text !== undefined) n.textContent = text;
  return n;
}

export function renderReview(root: HTMLElement, items: Review[], on: ReviewActions): void {
  root.replaceChildren();
  if (items.length === 0) {
    renderEmpty(root, "검토할 노트");
    return;
  }
  for (const it of items) {
    const stem = stemOf(it.path);
    const card = el("div", "card");

    const head = el("div", "card-head");
    head.append(
      el("span", "when", it.at.slice(0, 10)),
      el("span", "vault", vaultLabel(it.domain, it.vault)),
    );
    card.append(head);

    // 발췌 | 노트 — 나란히
    const split = el("div", "split");

    const left = el("div", "split-side");
    left.append(el("div", "split-title", "판별기가 본 발췌"));
    if (it.excerpt.trim() === "") {
      // **없는 것을 조용히 안 보여 주면 사람은 노트만 보고 맞다고 누른다.**
      // 옛 원장 줄(2026-08-12 이전)에는 발췌가 없다.
      left.append(el("p", "missing", "⚠️ 대조할 발췌가 없다 (옛 기록). 노트를 직접 열어 확인하라."));
    } else {
      const { shown, hidden } = clip(it.excerpt, EXCERPT_LINES);
      left.append(el("pre", "excerpt", shown));
      if (hidden > 0) left.append(el("div", "excerpt-more", `… ${hidden}줄 더`));
    }

    const right = el("div", "split-side");
    right.append(el("div", "split-title", "판별기가 쓴 노트"));
    right.append(el("div", "stem", stem), el("div", "path", it.path));

    split.append(left, right);
    card.append(split);

    const btns = el("div", "buttons");
    const ok = el("button", "primary", "맞다") as HTMLButtonElement;
    ok.addEventListener("click", () => on.ok(stem));
    const open = el("button", "secondary", "노트 열기") as HTMLButtonElement;
    open.addEventListener("click", () => on.open(stem));
    btns.append(ok, open);
    card.append(btns);

    root.append(card);
  }
}
```

`[고친다]`·`[지운다]` 는 v1 에 없다 — 스펙 §11 미결이고, 노트를 에디터에서 여는
것으로 대신한다. `prior review` 가 본문 절을 고치는 플래그를 갖고 있지 않다.

- [x] **Step 4: 스타일을 더한다**

`app/src/style.css` 맨 아래에 붙인다:

```css
.split { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
.split-side { min-width: 0; }
.split-title { font-size: 11px; color: #666; margin-bottom: 4px; }
.missing { color: #b45309; font-size: 11px; margin: 0; line-height: 1.5; }
.stem { font-size: 11px; word-break: break-all; }
.path { font-size: 10px; color: #888; word-break: break-all; margin-top: 2px; }
```

- [x] **Step 5: 테스트가 통과하는 것을 본다**

```bash
cd /Users/zesty/project/casebook/app
pnpm test 2>&1 | tail -6
```

Expected: `Tests  26 passed (26)`

- [x] **Step 6: 커밋**

```bash
cd /Users/zesty/project/casebook
git add app/src/render/review.ts app/tests/review.test.ts app/src/style.css
git commit -m "feat(app): 검토 큐 화면 — 발췌와 노트를 나란히

발췌가 없으면 **없다고 말한다.** 조용히 안 보여 주면 사람은 노트만 보고 맞다고
누르고, 그게 이 화면의 존재 이유(판별기 날조 검증)를 통째로 무너뜨린다.
옛 원장 줄(2026-08-12 이전)에는 발췌가 없다.

[고친다]·[지운다] 는 v1 에 없다 — prior review 가 본문 절을 고치는 플래그를
갖고 있지 않다. 노트를 에디터에서 여는 것으로 대신한다 (스펙 §11 미결)."
```

---

## Task 9: 회고 큐 화면과 상태 화면

**Files:**
- Create: `app/src/render/retro.ts`
- Create: `app/src/render/health.ts`
- Create: `app/tests/retro.test.ts`
- Create: `app/tests/health.test.ts`
- Modify: `app/src/style.css`

**Interfaces:**
- Consumes: `types.ts`, `format.ts`, `shell.ts`
- Produces:
  - `renderRetro(root, items: Retro[], on: RetroActions): void`
    where `interface RetroActions { judge(stem: string, outcome: "good" | "bad"): void }`
  - `renderHealth(root, items: Health[]): void`

- [x] **Step 1: 실패하는 테스트를 쓴다**

`app/tests/retro.test.ts`:

```ts
import { describe, it, expect, beforeEach, vi } from "vitest";
import { renderRetro } from "../src/render/retro";
import type { Retro } from "../src/types";

const base: Retro = {
  stem: "priorcase-결정-저장엔진-2026-08-01",
  date: "2026-08-01",
  domain: "priorcase",
  vault: "personal",
  summary: "저장 엔진을 임베디드 DB 로 고른다",
  author: "zesty <x@y.z>",
  reason: "recalled",
  hits: 4,
};

let root: HTMLElement;
const actions = { judge: vi.fn() };
beforeEach(() => {
  root = document.createElement("div");
  actions.judge.mockClear();
});

describe("renderRetro", () => {
  it("비었으면 빈 상태를 그린다", () => {
    renderRetro(root, [], actions);
    expect(root.textContent).toContain("회고할 결정");
  });

  it("재회수 횟수를 보여 준다", () => {
    renderRetro(root, [base], actions);
    expect(root.textContent).toContain("재회수 4회");
    expect(root.textContent).toContain("저장 엔진을 임베디드 DB 로 고른다");
  });

  // ★ superseded 는 hits 가 0 일 수 있다 — "재회수 0회" 는 거짓이다.
  it("뒤집힌 것은 횟수를 말하지 않는다", () => {
    renderRetro(root, [{ ...base, reason: "superseded", hits: 0 }], actions);
    expect(root.textContent).toContain("뒤집혔다");
    expect(root.textContent).not.toContain("0회");
  });

  it("좋았다/나빴다가 콜백을 부른다", () => {
    renderRetro(root, [base], actions);
    const btns = Array.from(root.querySelectorAll("button"));
    btns.find((b) => b.textContent === "좋았다")!.click();
    expect(actions.judge).toHaveBeenCalledWith(base.stem, "good");
    btns.find((b) => b.textContent === "나빴다")!.click();
    expect(actions.judge).toHaveBeenCalledWith(base.stem, "bad");
  });

  // ★ [아직] 은 명령을 부르지 않는다 — 같은 방아쇠가 한 번 더 울릴 때까지
  //   안 묻는 것이 규칙이고, 그건 재회수 카운트가 오르면 자연히 된다.
  it("아직 버튼은 아무 명령도 안 부른다", () => {
    renderRetro(root, [base], actions);
    const btn = Array.from(root.querySelectorAll("button"))
      .find((b) => b.textContent === "아직")!;
    btn.click();
    expect(actions.judge).not.toHaveBeenCalled();
  });
});
```

`app/tests/health.test.ts`:

```ts
import { describe, it, expect, beforeEach } from "vitest";
import { renderHealth } from "../src/render/health";
import type { Health } from "../src/types";

let root: HTMLElement;
beforeEach(() => {
  root = document.createElement("div");
});

describe("renderHealth", () => {
  // ★★ 큐가 셋 다 비었을 때 앱이 보여 줄 것이 이것뿐이다.
  //    비어 있어도 반드시 채운다.
  it("검사가 없으면 그 사실을 말한다", () => {
    renderHealth(root, []);
    expect(root.textContent).toContain("상태 검사를 받지 못했다");
  });

  it("등급별로 다른 표시를 붙인다", () => {
    renderHealth(root, [
      { name: "볼트 personal", level: "ok", detail: "/a" },
      { name: "팀 이식성", level: "warn", detail: "paths 로만 잡힌다", fix: "repos 를 더하라" },
      { name: "볼트 work", level: "fail", detail: "접근할 수 없다" },
    ]);
    const t = root.textContent!;
    expect(t).toContain("볼트 personal");
    expect(t).toContain("볼트 work");
    expect(t).toContain("repos 를 더하라");
  });

  // ★ 모르는 등급을 정상으로 뭉개면 하필 알아야 할 상태가 안 보인다.
  it("모르는 등급을 정상으로 그리지 않는다", () => {
    renderHealth(root, [{ name: "새검사", level: "unknown", detail: "x" }]);
    const row = root.querySelector(".health-row")!;
    expect(row.className).not.toContain("level-ok");
  });
});
```

- [x] **Step 2: 테스트가 실패하는 것을 본다**

```bash
cd /Users/zesty/project/casebook/app
pnpm test 2>&1 | tail -6
```

Expected: `Failed to resolve import "../src/render/retro"`

- [x] **Step 3: 최소 구현을 쓴다**

`app/src/render/retro.ts`:

```ts
import type { Retro } from "../types";
import { vaultLabel, reasonLabel } from "../format";
import { renderEmpty } from "./shell";

export interface RetroActions {
  judge(stem: string, outcome: "good" | "bad"): void;
}

function el(tag: string, cls: string, text?: string): HTMLElement {
  const n = document.createElement(tag);
  n.className = cls;
  if (text !== undefined) n.textContent = text;
  return n;
}

export function renderRetro(root: HTMLElement, items: Retro[], on: RetroActions): void {
  root.replaceChildren();
  if (items.length === 0) {
    renderEmpty(root, "회고할 결정");
    return;
  }
  for (const it of items) {
    const card = el("div", "card");

    const head = el("div", "card-head");
    head.append(
      el("span", "when", it.date),
      el("span", "vault", vaultLabel(it.domain, it.vault)),
      el("span", "reason", reasonLabel(it.reason, it.hits)),
    );
    card.append(head);

    card.append(el("div", "summary", it.summary));
    if (it.author) card.append(el("div", "author", `— ${it.author}`));

    const btns = el("div", "buttons");
    const good = el("button", "primary", "좋았다") as HTMLButtonElement;
    good.addEventListener("click", () => on.judge(it.stem, "good"));
    const bad = el("button", "secondary", "나빴다") as HTMLButtonElement;
    bad.addEventListener("click", () => on.judge(it.stem, "bad"));
    // **[아직] 은 아무 명령도 부르지 않는다.** 같은 방아쇠가 한 번 더 울릴
    // 때까지 안 묻는 것이 규칙이고, 그건 재회수 카운트가 오르면 자연히 된다.
    // 상태를 따로 저장하면 규칙이 둘이 되고 "왜 안 뜨지" 를 설명할 수 없다.
    const later = el("button", "secondary", "아직") as HTMLButtonElement;
    later.addEventListener("click", () => {
      card.remove();
      if (root.children.length === 0) renderEmpty(root, "회고할 결정");
    });
    btns.append(good, bad, later);
    card.append(btns);

    root.append(card);
  }
}
```

`app/src/render/health.ts`:

```ts
import type { Health, Level } from "../types";

const MARK: Record<Level, string> = { ok: "✓", warn: "⚠", fail: "✗", unknown: "?" };

function el(tag: string, cls: string, text?: string): HTMLElement {
  const n = document.createElement(tag);
  n.className = cls;
  if (text !== undefined) n.textContent = text;
  return n;
}

/** renderHealth 는 상태 검사를 그린다.
 *
 * **큐가 셋 다 비었을 때 앱이 보여 줄 것이 이것뿐이다.** 비어 있어도 반드시
 * 채운다 — 빈 화면은 "고장난 것처럼" 보인다. */
export function renderHealth(root: HTMLElement, items: Health[]): void {
  root.replaceChildren();
  if (items.length === 0) {
    root.append(el("p", "empty", "상태 검사를 받지 못했다. prior 가 도는지 확인하라."));
    return;
  }
  for (const it of items) {
    // 모르는 등급을 정상으로 뭉개지 않는다 — 하필 알아야 할 상태가 안 보인다.
    const lvl: Level = it.level === "ok" || it.level === "warn" || it.level === "fail"
      ? it.level
      : "unknown";
    const row = el("div", `health-row level-${lvl}`);
    row.append(
      el("span", "health-mark", MARK[lvl]),
      el("span", "health-name", it.name),
      el("span", "health-detail", it.detail),
    );
    if (it.fix) row.append(el("div", "health-fix", `→ ${it.fix}`));
    root.append(row);
  }
}
```

- [x] **Step 4: 스타일을 더한다**

`app/src/style.css` 맨 아래에 붙인다:

```css
.summary { line-height: 1.5; margin-bottom: 4px; }
.author { font-size: 11px; color: #888; }
.reason { font-weight: 600; color: inherit; }
.health-row { display: grid; grid-template-columns: 16px 120px 1fr; gap: 8px; align-items: baseline; padding: 4px 0; font-size: 12px; }
.health-row .health-fix { grid-column: 2 / -1; font-size: 11px; color: #666; }
.level-ok .health-mark { color: #16a34a; }
.level-warn .health-mark { color: #d97706; }
.level-fail .health-mark { color: #dc2626; }
.level-unknown .health-mark { color: #6b7280; }
.health-detail { color: #555; }
```

- [x] **Step 5: 테스트가 통과하는 것을 본다**

```bash
cd /Users/zesty/project/casebook/app
pnpm test 2>&1 | tail -6
```

Expected: `Tests  34 passed (34)`

- [x] **Step 6: 커밋**

```bash
cd /Users/zesty/project/casebook
git add app/src/render app/tests app/src/style.css
git commit -m "feat(app): 회고 큐와 상태 화면

[아직] 은 아무 명령도 부르지 않는다. 같은 방아쇠가 한 번 더 울릴 때까지 안 묻는
것이 규칙이고 그건 재회수 카운트가 오르면 자연히 된다 — 상태를 따로 저장하면
규칙이 둘이 되고 '왜 안 뜨지' 를 설명할 수 없다.

상태는 검사가 없어도 반드시 무언가를 그린다. 큐가 셋 다 비었을 때 앱이 보여 줄
것이 이것뿐이라, 빈 화면은 '고장난 것처럼' 보인다.

모르는 등급을 정상으로 뭉개지 않는다."
```

---

## Task 10: 조립 — 탭·폴링·트레이 숫자

**Files:**
- Modify: `app/src/main.ts`
- Modify: `app/src-tauri/src/main.rs` (트레이 제목 갱신 커맨드)
- Modify: `app/src-tauri/src/commands.rs` (`set_tray_title`, `open_note`)
- Create: `app/tests/main.test.ts`
- Modify: `app/src/style.css`

**Interfaces:**
- Consumes: 화면 넷 (Task 6·7·8), `api.ts` (Task 5)
- Produces:
  - `commands::set_tray_title(app: tauri::AppHandle, title: String)`
  - `commands::open_note(stem: String) -> Result<(), CmdError>` — `prior path <stem>` 으로 경로를 얻어 `open` 한다
  - `main.ts`: `badgeText(q: Queue): string` (테스트 대상), 폴링 루프

- [x] **Step 1: 실패하는 테스트를 쓴다**

`app/tests/main.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { badgeText } from "../src/main";
import type { Queue } from "../src/types";

const empty: Queue = { confirm: [], review: [], retro: [], health: [] };

describe("badgeText", () => {
  // ★ 할 일이 없으면 숫자를 안 붙인다. 늘 무언가 떠 있으면 사람이 무시한다.
  it("할 일이 없으면 빈 문자열", () => {
    expect(badgeText(empty)).toBe("");
  });

  it("종류별 개수를 짧게 붙인다", () => {
    const q: Queue = {
      ...empty,
      confirm: [{} as any, {} as any, {} as any],
      retro: [{} as any, {} as any],
    };
    expect(badgeText(q)).toBe("확인 3 · 회고 2");
  });

  it("검토도 센다", () => {
    expect(badgeText({ ...empty, review: [{} as any] })).toBe("검토 1");
  });

  // ★★ 상태에 fail 이 있으면 큐가 비어도 알린다 — 고장이 조용하면 안 된다.
  it("고장은 큐가 비어도 알린다", () => {
    const q: Queue = { ...empty, health: [{ name: "볼트", level: "fail", detail: "x" }] };
    expect(badgeText(q)).toBe("⚠");
  });
});
```

- [x] **Step 2: 테스트가 실패하는 것을 본다**

```bash
cd /Users/zesty/project/casebook/app
pnpm test 2>&1 | tail -6
```

Expected: `does not provide an export named 'badgeText'`

- [x] **Step 3: Rust 커맨드 둘을 더한다**

`app/src-tauri/src/commands.rs` 맨 아래(테스트 모듈 위)에 넣는다:

```rust
/// set_tray_title 은 메뉴바 아이콘 옆의 글자를 바꾼다.
///
/// **푸시 알림을 보내지 않는다.** 먼저 말을 거는 도구는 지워지고, 앱이 지워지면
/// 자동 기록까지 같이 잃는다. 숫자만 조용히 바꾼다.
#[tauri::command]
pub fn set_tray_title(app: tauri::AppHandle, title: String) {
    use tauri::Manager;
    if let Some(tray) = app.tray_by_id("main") {
        let _ = tray.set_title(Some(if title.is_empty() { "".to_string() } else { title }));
    }
}

/// open_note 는 결정 노트를 기본 앱으로 연다.
///
/// **앱이 볼트 경로를 조립하지 않는다.** `prior path <stem>` 에게 절대 경로를
/// 묻고 그것을 OS 에 넘긴다 — 볼트 선택 규칙이 둘이 되면 다중 볼트에서 앱이
/// 엉뚱한 파일을 열거나 못 연다.
#[tauri::command]
pub fn open_note(stem: String) -> Result<(), CmdError> {
    let out = run(&prior_bin(), &["path", &stem], READ_TIMEOUT).map_err(to_cmd_error)?;
    let path = out.trim();
    if path.is_empty() {
        return Err(CmdError {
            kind: "failed".into(),
            message: format!("prior path 가 빈 줄을 냈다 (stem={stem})"),
        });
    }
    std::process::Command::new("open")
        .arg(path)
        .spawn()
        .map(|_| ())
        .map_err(|e| CmdError { kind: "io".into(), message: e.to_string() })
}
```

`main.rs` 의 `generate_handler!` 에 둘을 더한다:

```rust
            commands::set_tray_title,
            commands::open_note,
```

- [x] **Step 4: 프런트를 조립한다**

`app/src/main.ts` 를 통째로 바꾼다:

```ts
import { invoke } from "@tauri-apps/api/core";
import { fetchQueue, resolvePending, promote, review } from "./api";
import type { Queue, CmdError } from "./types";
import { renderConfirm } from "./render/confirm";
import { renderReview } from "./render/review";
import { renderRetro } from "./render/retro";
import { renderHealth } from "./render/health";
import { renderError, renderWarnings, renderEmpty } from "./render/shell";

type Tab = "confirm" | "review" | "retro" | "health";

const LABEL: Record<Tab, string> = {
  confirm: "확인",
  review: "검토",
  retro: "회고",
  health: "상태",
};

/** badgeText 는 메뉴바에 붙일 글자다.
 *
 * **할 일이 없으면 빈 문자열이다.** 늘 무언가 떠 있으면 사람이 그것을 무시하는
 * 법을 배우고, 그러면 진짜 할 일이 있을 때도 안 보인다.
 *
 * 상태에 fail 이 있으면 큐가 비어도 알린다 — 고장이 조용하면 안 된다. */
export function badgeText(q: Queue): string {
  if (q.health.some((h) => h.level === "fail")) return "⚠";
  const parts: string[] = [];
  if (q.confirm.length > 0) parts.push(`확인 ${q.confirm.length}`);
  if (q.review.length > 0) parts.push(`검토 ${q.review.length}`);
  if (q.retro.length > 0) parts.push(`회고 ${q.retro.length}`);
  return parts.join(" · ");
}

/** POLL_MS 는 폴링 주기다.
 *
 * 파일 감시(fsnotify)를 안 쓰는 이유: 볼트 여럿과 상태 디렉토리를 다 봐야 하고,
 * 그래도 회고 큐는 재계산해야 한다. 얻는 것이 적다. */
const POLL_MS = 30_000;

const app = document.querySelector<HTMLDivElement>("#app")!;
let tab: Tab = "confirm";
let last: Queue | null = null;

function shell(): { tabs: HTMLElement; banner: HTMLElement; body: HTMLElement } {
  app.replaceChildren();
  const tabs = document.createElement("nav");
  tabs.className = "tabs";
  const banner = document.createElement("div");
  const body = document.createElement("div");
  body.className = "body";
  app.append(tabs, banner, body);
  return { tabs, banner, body };
}

function draw(q: Queue): void {
  const { tabs, banner, body } = shell();

  for (const t of ["confirm", "review", "retro", "health"] as Tab[]) {
    const n = t === "health" ? 0 : q[t].length;
    const b = document.createElement("button");
    b.className = "tab" + (t === tab ? " active" : "");
    b.textContent = n > 0 ? `${LABEL[t]} ${n}` : LABEL[t];
    b.addEventListener("click", () => {
      tab = t;
      draw(q);
    });
    tabs.append(b);
  }

  renderWarnings(banner, q.warnings);

  const act = async (fn: () => Promise<void>) => {
    try {
      await fn();
      await refresh();
    } catch (e) {
      // **쓰기가 실패하면 조용히 넘어가지 않는다.** 큐에서 사라졌는데 볼트에
      // 반영이 안 되면 사람은 답한 줄 안다.
      renderError(body, e as CmdError);
    }
  };

  switch (tab) {
    case "confirm":
      renderConfirm(body, q.confirm, {
        resolve: (id) => void act(() => resolvePending(id)),
        promote: (id) => void act(() => promote(id)),
      });
      break;
    case "review":
      renderReview(body, q.review, {
        ok: (stem) => void act(() => review(stem, "good")),
        open: (stem) => void act(async () => {
          await invoke("open_note", { stem });
        }),
      });
      break;
    case "retro":
      renderRetro(body, q.retro, {
        judge: (stem, outcome) => void act(() => review(stem, outcome)),
      });
      break;
    case "health":
      renderHealth(body, q.health);
      break;
  }
}

async function refresh(): Promise<void> {
  try {
    const q = await fetchQueue();
    last = q;
    await invoke("set_tray_title", { title: badgeText(q) });
    draw(q);
  } catch (e) {
    // **빈 큐로 그리지 않는다.** 고장이 "할 일 없음" 이 되면 앱도 이 시스템의
    // 병(고장이 정상과 구별되지 않음)에 걸린다.
    const { banner, body } = shell();
    renderWarnings(banner, undefined);
    renderError(body, e as CmdError);
    await invoke("set_tray_title", { title: "⚠" });
  }
}

// 앱이 뜰 때 한 번, 그리고 주기적으로.
void refresh();
setInterval(() => void refresh(), POLL_MS);
// 창을 열 때도 갱신한다 — 30초를 기다리게 하면 오래된 것을 보게 된다.
window.addEventListener("focus", () => void refresh());

// 테스트가 마지막 큐를 볼 수 있게 (개발 편의).
export function lastQueue(): Queue | null {
  return last;
}
```

- [x] **Step 5: 탭 스타일을 더한다**

`app/src/style.css` 맨 아래에 붙인다:

```css
.tabs { display: flex; gap: 4px; margin-bottom: 10px; border-bottom: 1px solid #0002; }
.tab { border: none; background: none; border-radius: 0; border-bottom: 2px solid transparent; padding: 6px 10px; }
.tab.active { border-bottom-color: #2563eb; font-weight: 600; }
.body { min-height: 200px; }
```

- [x] **Step 6: 테스트가 통과하는 것을 본다**

`main.ts` 가 최상위에서 `invoke` 를 부르므로 테스트에서 모킹이 필요하다.
`app/tests/main.test.ts` 맨 위에 넣는다:

```ts
import { vi } from "vitest";
vi.mock("@tauri-apps/api/core", () => ({ invoke: vi.fn().mockResolvedValue("{}") }));
```

```bash
cd /Users/zesty/project/casebook/app
pnpm test 2>&1 | tail -6
```

Expected: `Tests  38 passed (38)`

- [x] **Step 7: 컴파일과 빌드를 확인한다**

```bash
cd /Users/zesty/project/casebook/app
pnpm exec tsc --noEmit
cargo check --manifest-path src-tauri/Cargo.toml
```

Expected: 둘 다 에러 없음

- [x] **Step 8: 커밋**

```bash
cd /Users/zesty/project/casebook
git add app
git commit -m "feat(app): 탭·폴링·트레이 숫자 조립

badgeText 는 할 일이 없으면 빈 문자열이다 — 늘 무언가 떠 있으면 사람이 무시하는
법을 배우고, 그러면 진짜 할 일이 있을 때도 안 보인다. 상태에 fail 이 있으면 큐가
비어도 ⚠ 를 띄운다.

큐를 못 받으면 빈 큐로 그리지 않는다. 고장이 '할 일 없음' 이 되면 앱도 이
시스템의 병(고장이 정상과 구별되지 않음)에 걸린다.

쓰기가 실패하면 그 자리에 이유를 그린다."
```

---

## Task 11: 오류 상황 여섯을 가짜 prior 로 재현

**Files:**
- Create: `app/fixtures/fake-prior-warnings.sh`
- Create: `app/fixtures/fake-prior-onebroken.sh`
- Create: `app/tests/errors.test.ts`

**Interfaces:**
- Consumes: `render/shell.ts`, `api.ts`
- Produces: 스펙 §6 표의 여섯 행이 그대로 테스트 여섯 개

- [ ] **Step 1: 가짜 스크립트 둘을 더한다**

```bash
cd /Users/zesty/project/casebook/app
cat > fixtures/fake-prior-warnings.sh <<'SH'
#!/bin/sh
cat <<'JSON'
{"confirm":[],"review":[],"retro":[],
 "health":[{"name":"볼트 personal","level":"ok","detail":"/a"}],
 "warnings":["볼트 work 에 접근할 수 없다"]}
JSON
SH

cat > fixtures/fake-prior-onebroken.sh <<'SH'
#!/bin/sh
cat <<'JSON'
{"confirm":[],"review":[],
 "retro":[{"stem":"a-결정-x-2026-08-01","date":"2026-08-01","domain":"a","vault":"personal",
           "summary":"살아 있는 볼트의 결정","reason":"recalled","hits":2}],
 "health":[{"name":"볼트 personal","level":"ok","detail":"/a"},
           {"name":"볼트 work","level":"fail","detail":"접근할 수 없다"}],
 "warnings":["회고 큐를 만들지 못했다: 볼트 work 에 접근할 수 없다"]}
JSON
SH
chmod +x fixtures/*.sh
ls fixtures/
```

Expected: 여섯 개 스크립트가 보인다

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`app/tests/errors.test.ts`:

```ts
import { describe, it, expect, beforeEach } from "vitest";
import { renderError, renderWarnings } from "../src/render/shell";
import { renderRetro } from "../src/render/retro";
import { renderHealth } from "../src/render/health";
import type { Queue, CmdError } from "../src/types";

// 스펙 §6 오류 처리 표의 여섯 행이 그대로 여섯 테스트다.
// **빈 큐와 실패를 구별하는 것이 이 표의 전부다.**

let root: HTMLElement;
beforeEach(() => {
  root = document.createElement("div");
});

describe("스펙 §6 오류 처리", () => {
  it("① prior 를 못 찾으면 설치 안내만 보여 준다", () => {
    renderError(root, { kind: "not_found", message: "prior 를 찾을 수 없다" });
    expect(root.textContent).toContain("설치");
    expect(root.textContent).not.toContain("할 일이 없");
  });

  it("② 0 아닌 종료는 stderr 를 그대로 보여 준다", () => {
    const e: CmdError = { kind: "failed", message: "설정에 알 수 없는 키가 있다 (/x.toml)" };
    renderError(root, e);
    expect(root.textContent).toContain("/x.toml");
  });

  it("③ warnings 가 있으면 배너를 낸다", () => {
    renderWarnings(root, ["볼트 work 에 접근할 수 없다"]);
    expect(root.textContent).toContain("불완전");
    expect(root.textContent).toContain("볼트 work");
  });

  it("④ 쓰기 실패는 이유를 보여 준다", () => {
    renderError(root, { kind: "failed", message: "승격이 일어나지 않았다 (/t.jsonl@0)" });
    expect(root.textContent).toContain("승격이 일어나지 않았다");
  });

  // ★★ 볼트 하나가 깨져도 **나머지 볼트의 큐는 그대로 보여 준다.**
  it("⑤ 볼트 하나가 깨져도 나머지를 그린다", () => {
    const q: Queue = JSON.parse(`{"confirm":[],"review":[],
      "retro":[{"stem":"a-결정-x-2026-08-01","date":"2026-08-01","domain":"a","vault":"personal",
                "summary":"살아 있는 볼트의 결정","reason":"recalled","hits":2}],
      "health":[{"name":"볼트 personal","level":"ok","detail":"/a"},
                {"name":"볼트 work","level":"fail","detail":"접근할 수 없다"}],
      "warnings":["회고 큐를 만들지 못했다: 볼트 work"]}`);
    const banner = document.createElement("div");
    const body = document.createElement("div");
    renderWarnings(banner, q.warnings);
    renderRetro(body, q.retro, { judge: () => {} });
    expect(banner.textContent).toContain("불완전");
    // 나머지 볼트의 회고는 살아 있어야 한다
    expect(body.textContent).toContain("살아 있는 볼트의 결정");
  });

  it("⑥ 깨진 JSON 은 빈 큐가 아니다", () => {
    // api.fetchQueue 가 던지는 모양을 그대로 재현한다.
    const e: CmdError = { kind: "failed", message: 'prior 가 JSON 이 아닌 것을 냈다: {"confirm":[ this' };
    renderError(root, e);
    expect(root.textContent).toContain("JSON 이 아닌");
    expect(root.textContent).not.toContain("할 일이 없");
  });
});

describe("빈 상태는 고장처럼 보이지 않는다", () => {
  it("큐가 셋 다 비어도 상태가 채워진다", () => {
    renderHealth(root, [{ name: "볼트 personal", level: "ok", detail: "/a" }]);
    expect(root.textContent).toContain("볼트 personal");
    expect(root.querySelector(".level-ok")).not.toBeNull();
  });
});
```

- [ ] **Step 3: 테스트를 돌린다**

```bash
cd /Users/zesty/project/casebook/app
pnpm test 2>&1 | tail -6
```

Expected: `Tests  45 passed (45)` — 이 테스트들은 Task 5·8 의 구현으로 이미 통과한다.
하나라도 실패하면 그 화면이 스펙 §6 을 안 지키는 것이므로 화면 코드를 고친다.

- [ ] **Step 4: 가짜 prior 로 실제 경로를 눈으로 확인한다**

```bash
cd /Users/zesty/project/casebook/app
for f in ok broken-json exit1 warnings onebroken; do
  echo "── $f"
  PRIORCASE_APP_BIN="$PWD/fixtures/fake-prior-$f.sh" pnpm tauri dev &
  sleep 12
  kill %1 2>/dev/null
done
```

각 실행에서 창을 열어 화면을 눈으로 본다:

| 가짜 | 보여야 하는 것 |
| --- | --- |
| `ok` | 빈 상태 넷 + 상태 탭에 볼트 한 줄. 메뉴바에 글자 없음 |
| `broken-json` | "prior 가 JSON 이 아닌 것을 냈다" + 원문 앞부분. 메뉴바 ⚠ |
| `exit1` | stderr 원문. 메뉴바 ⚠ |
| `warnings` | 노란 배너 "큐가 불완전하다" + 빈 큐 |
| `onebroken` | 배너 + 회고 1건 + 상태에 `볼트 work ✗`. 메뉴바 ⚠ |

- [ ] **Step 5: 커밋**

```bash
cd /Users/zesty/project/casebook
git add app/fixtures app/tests/errors.test.ts
git commit -m "test(app): 스펙 §6 오류 처리 여섯 행을 테스트 여섯 개로

빈 큐와 실패를 구별하는 것이 그 표의 전부다. 이 시스템은 모든 부품이 실패해도
대화를 막지 않도록 설계됐고, 그 대가로 고장이 정상과 구별되지 않는다 —
앱이 그 구별을 못 하면 앱도 같은 병에 걸린다.

볼트 하나가 깨져도 나머지 볼트의 큐는 그대로 그린다."
```

---

## Task 12: 실기기 검증과 `.app` 빌드

**Files:**
- Create: `app/README.md`
- Modify: `docs/superpowers/specs/2026-08-12-데스크탑-감독앱-design.md` (완료 표시)

**Interfaces:**
- Consumes: 전체
- Produces: `app/src-tauri/target/release/bundle/macos/priorcase.app`

- [ ] **Step 1: 진짜 prior 로 눈으로 확인한다**

```bash
cd /Users/zesty/project/casebook/app
PRIORCASE_APP_BIN="$HOME/.local/bin/prior" pnpm tauri dev
```

확인할 것:

| | 기대 |
| --- | --- |
| 메뉴바 | 실제 큐 수가 붙는다 (예: `확인 12 · 검토 3 · 회고 45`) |
| 확인 탭 | 발췌가 접히고 "… N줄 더" 가 보인다. 비슷한 결정에 점수가 붙는다 |
| 검토 탭 | 발췌와 노트 stem 이 나란히. 옛 줄은 "대조할 발췌가 없다" |
| 회고 탭 | "재회수 N회" 또는 "뒤집혔다" |
| 상태 탭 | `prior doctor` 와 같은 줄들 |
| `[아직]` | 그 카드만 사라지고 명령은 안 돈다 |

**아무것도 누르지 마라.** 실제 볼트가 바뀐다. 눌러 볼 것이 있으면 먼저
`prior queue --json` 으로 그 항목을 확인하고, 되돌릴 수 있는 것만 누른다.

- [ ] **Step 2: `.app` 을 만든다**

```bash
cd /Users/zesty/project/casebook/app
pnpm tauri build --bundles app 2>&1 | tail -20
ls -la src-tauri/target/release/bundle/macos/
```

Expected: `priorcase.app` 이 생긴다. DMG 는 만들지 않는다 — 비대화형 세션에서 멈춘다.

- [ ] **Step 3: 격리 속성을 떼고 실행한다**

```bash
cd /Users/zesty/project/casebook/app
xattr -dr com.apple.quarantine src-tauri/target/release/bundle/macos/priorcase.app
open src-tauri/target/release/bundle/macos/priorcase.app
```

Expected: 공증 없이도 열린다. 메뉴바에 아이콘이 뜬다.

- [ ] **Step 4: README 를 쓴다**

`app/README.md`:

```markdown
# priorcase 데스크탑 감독 앱

기계가 한 일(자동 기록)을 사람이 클릭 한 번으로 승인·정정·판정하는 macOS
메뉴바 상주 앱.

설계: `docs/superpowers/specs/2026-08-12-데스크탑-감독앱-design.md`

## 개발

```bash
pnpm install
pnpm tauri dev
```

가짜 `prior` 로 오류 화면을 보려면:

```bash
PRIORCASE_APP_BIN="$PWD/fixtures/fake-prior-exit1.sh" pnpm tauri dev
```

## 테스트

```bash
pnpm test                                    # 프런트
cargo test --manifest-path src-tauri/Cargo.toml --lib   # Rust
```

## 빌드

```bash
pnpm tauri build --bundles app
xattr -dr com.apple.quarantine src-tauri/target/release/bundle/macos/priorcase.app
```

**공증하지 않는다.** v1 은 내가 쓰는 것이고, 한두 명에게 줄 때 다시 본다.
DMG 는 만들지 않는다 — 번들링이 비대화형 세션에서 멈춘다.

## 규칙

**앱은 `prior` 명령만 부른다.** 볼트 파일을 직접 읽거나 쓰지 않는다 —
frontmatter 방출·스키마 검증·색인 갱신·볼트 선택이 두 벌이 되면 한쪽만
고쳐진 채로 남는다.
```

- [ ] **Step 5: 스펙에 완료를 적는다**

`docs/superpowers/specs/2026-08-12-데스크탑-감독앱-design.md` 의 제목 아래
개정 블록 끝에 한 줄 더한다:

```markdown
> **2026-08-13 v1 구현 완료** — 계획은
> `docs/superpowers/plans/2026-08-13-데스크탑-감독앱.md`.
> `[고친다]`·`[지운다]` 는 v1 에 없다 (§11 미결).
```

- [ ] **Step 6: 커밋**

```bash
cd /Users/zesty/project/casebook
git add app/README.md docs/superpowers/specs
git commit -m "docs(app): README 와 스펙 완료 표시

v1 은 공증 없이 .app 으로 빌드한다. xattr 로 격리를 떼면 열린다."
```

---

## 자기 검토

**1. 스펙 커버리지**

| 스펙 절 | 태스크 |
| --- | --- |
| §1 무엇을 만드는가 | 전체 |
| §2 형태 (메뉴바 · 알림 없음) | Task 1(트레이), Task 10(`set_tray_title`) |
| §3 볼트가 여럿 | Task 4(`vaultLabel`), 화면 넷이 `vault` 를 그린다 |
| §4.1 확인 큐 | Task 6 |
| §4.2 검토 큐 | Task 8 |
| §4.3 회고 큐 | Task 9 |
| §4.4 상태 | Task 9 |
| §5 아키텍처 (CLI 경유) | Task 2·3·7, Global Constraints |
| §5.1 JSON 계약 | Task 4 (`types.ts`) |
| §5.2 폴링 30초 | Task 10 (`POLL_MS`) |
| §5.3 쓰기 실패를 드러냄 | Task 10 (`act`) |
| §6 오류 처리 6행 | Task 5(구현) · Task 11(테스트 6개) |
| §7 테스트 | Task 11 |
| §8 배포 (공증 없음) | Task 12 |
| §9 범위 밖 | 계획에 없음 — 의도한 것 |
| §11 미결 `[고친다]` | Task 8 에서 "노트 열기" 로 대신하고 명시 |

**빠진 것 하나**: §4.4 의 "배선이 안 됐으면 `prior init --apply` 를 대신 실행한다".
`init` 은 `~/.claude/settings.json` 을 고치는 명령이라 앱에서 부르면 되돌리기
어렵고, `--revert` 가 있긴 하지만 v1 에 넣을 값이 낮다. **v1 범위 밖으로 둔다** —
상태 탭이 `fix` 문구를 그리므로 사람이 터미널에서 그대로 친다.

**2. 플레이스홀더 스캔**

"TBD"·"적절히"·"에러 처리를 추가한다" 없음. 모든 코드 단계에 실제 코드가 있다.

계획을 쓰면서 가정 하나가 틀린 것을 확인해 고쳤다 — 처음에는 `prior doctor --json`
으로 볼트 경로를 얻으려 했는데 **그 플래그가 없다.** 상태 검사의 `detail` 에 볼트
경로가 들어 있지만 그건 사람이 읽는 문구라 앱이 파싱하면 문구를 못 고치게 된다.
`prior path <stem>` (Task 7)을 더해 경로 해석을 CLI 에 남겼다.

**3. 타입 일관성**

- `Confirm.gave_up`(snake_case) — Go 의 `json:"gave_up"` 과 일치. Task 4·6 에서 같은 이름.
- `ConfirmActions {resolve, promote}` — Task 6 정의, Task 10 사용. 일치.
- `ReviewActions {ok, open}` — 둘 다 `stem: string` 을 받는다. Task 8 정의, Task 10 사용. 일치.
- `RetroActions {judge}` — Task 9 정의, Task 10 사용. 일치.
- `CmdError.kind` 값 넷(`not_found|failed|timeout|io`) — Rust(Task 3)와 TS(Task 4)가 같다.
- `READ_TIMEOUT`/`WRITE_TIMEOUT` — Task 3 정의, Task 3 테스트가 검사.
- `stemOf` — Task 8 정의·사용. `prior path` 가 받는 stem 과 같은 값이다.
- `open_note(stem)` — Task 10 정의(Rust)·사용(TS). `open_path` 가 아니다.
