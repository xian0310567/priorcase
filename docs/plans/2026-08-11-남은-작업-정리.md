# priorcase — 완료까지 남은 작업

> 2026-08-11 실측 기준. 기억이 아니라 코드·볼트·레지스트리를 재서 만들었다.

## 지금 상태

| | |
| --- | --- |
| v1 스펙 명령어 | **전부 구현됨** (스펙에만 있고 미구현인 명령어 0개) |
| 테스트 | 18개 패키지 통과 · 커버리지 **79.3%** |
| 배포 | npm `priorcase@0.1.2` · 태그 push 로 CI 가 게시 (인증 0회) |
| 실사용 | 이 볼트에서 도는 중 · 결정 노트 93건 |

**v1 은 사실상 닫혔다.** 남은 것은 (A) 팀이 쓸 수 있게 만드는 것, (B) 스펙이
v2 로 미룬 것, (C) 품질 부채다.

---

## A. 팀 도입 차단 요소 — ✅ 완료 (2026-08-12)

A1·A2·A3 을 한 브랜치에서 끝냈다. A4 는 아직 남았다.

### A1. 도메인 해석을 경로에서 git remote 로 — ✅

지금 `DomainForCwd` 는 설정의 **절대 경로**로 판정한다
(`config.go:265`, `paths = ["~/project/priorcase"]`).

팀원마다 체크아웃 경로가 다르므로 **새 사람이 설정 파일을 손으로 고쳐야 한다.**
`npm i -g priorcase` 직후 첫 경험이 그것이 된다.

`git remote get-url origin` → `xian0310567/priorcase` 는 누구 기계에서든 같다.
경로 매칭은 폴백으로 남긴다 (git 이 아닌 디렉토리도 있다).

### A2. `author` — 누가 내린 결정인가 — ✅

frontmatter 10키에 없다 (`frontmatter.go:14-23`). 팀에서는 "이 결정 누가 왜"가
회수의 절반이다. `Extra` 인라인으로 들어가긴 하지만 스키마·검색 대상이 아니다.

`schema: 2` 로 올리고 `author` 를 정식 키로. 판 1 노트는 그대로 읽힌다
(`IsFuture` 가 이미 있다).

### A3. git 공유 시 충돌하는 자리 — ✅

볼트를 사설 git 레포로 공유할 때:

- `_meta/00-결정-색인.md` — **24KB 를 매번 통째로 재생성.** 두 사람이 각자
  `prior capture` 하면 매번 충돌한다.
- `99-<프로젝트>-작업-로그.md` — 여러 사람이 파일 끝에 append → 충돌.

색인을 gitignore 하고 각자 `prior index` 로 만들게 하거나, 결정 노트에서
파생 가능한 형태로 바꾼다. 작업 로그는 날짜별 파일로 쪼개는 방법이 있다.

### A4. 볼트 분리 — 프로젝트가 10개 섞여 있다 (남음)

`~/Documents/Obsidian Vault` 에 `NOI`(220건) 포함 10개 프로젝트가 있다.
통째로 공유하면 전부 노출된다. **프로젝트 폴더 단위 공유 구조**를 정해야 한다.

지금 `priorcase` 가 안 하는 일이다. 설계 결정이 먼저 필요하다.

---

## B. 스펙이 v2 로 미룬 것

### B1. 다른 호스트 파서 ★ — Codex CLI 완료 (2026-08-12)

`internal/transcript/` 에 `codex` 를 붙이고 `hosts` 레지스트리로 다중 루트 배선을
세웠다. 데몬이 Claude Code(1,621) + Codex(1,729) = 3,350 파일을 함께 감시한다.

실측이 후보를 갈랐다:

| 호스트 | 로컬 대화 | 세션 | 상태 |
| --- | --- | --- | --- |
| Codex CLI | `~/.codex/sessions/**/*.jsonl` | 1,729 | ✅ |
| Gemini CLI | `~/.gemini/tmp/<hash>/chats/*.json` | 1 | 남음 — `cwd` 없이 `projectHash` 뿐이라 도메인 매핑 설계가 선행 |
| Cursor | `state.vscdb` (문서화 안 된 SQLite BLOB) | ? | 남음 — 추출 가능성부터 재야 한다 |
| Claude Desktop | LevelDB 캐시뿐, 대화는 서버 | — | **로컬 정본이 없다.** 파서로는 안 된다 |

상세는 볼트 `priorcase-결정-호스트파서-codex-먼저-2026-08-12`.

**남은 사각 하나**: 세션 중간부터 읽으면 Codex 는 `cwd` 를 못 잡는다
(`session_meta` 가 첫 줄에만 있다). 도메인이 기본값으로 떨어진다.

### B2. 임베딩 검색

지금 회수는 파일명·summary·tags 만 본다 (`search.scoreAll` 의
`headHits == 0 → continue`). 유료층 후보였으나 E2E 와 충돌한다는 결정이 있다.

### B3. 데스크탑 앱 — 우선순위 상향 (2026-08-12)

**음성을 담을 그릇으로서가 아니라 그 자체로 값이 있어서 올린다.** 지금 새 사용자는
`prior init --apply` 를 직접 치고, 무엇이 잡히는지 보려면 `prior doctor` 를 친다.
팀 도입의 첫 마찰이 거기다. 앱이 배선·상태·미확인 구간을 맡으면 온보딩이 달라진다.

**CLI + 훅을 대체하지 않는다.** 앱이 그걸 감싼다 — 둘 중 하나를 고르는 것이 아니다.
그리고 `.app` 배포는 **애플 공증을 되살린다**(npm 경로가 없앴던 비용).

### B4. 협업 도구 소스 · 회의 음성 — 순서 뒤

Jira·Slack·Asana 와 회의 음성까지 소스로. **기준은 "그 소스에 결정이 있는가" 이지
"데이터가 있는가" 가 아니다.** 전부 색인으로 가면 예산이 훨씬 큰 회사들의 링이다.
음성은 참가자 동의 문제가 기술보다 먼저 막을 수 있다.

### B5. 화면 녹화 포착 — 보류

데스크탑을 통째로 녹화해 결정을 뽑는 안. 문제의식은 측정된 것이지만 위 단들이
훨씬 값싸다. 중간 단계로 "창 제목·앱 전환만"(픽셀 없이)도 후보다. 상세는 볼트
`priorcase-결정-화면녹화포착-파서먼저-보류-2026-08-12`.

### B6. 자정 작용 (주제 전환 감지)

스펙이 "규칙으로 판정하기 어렵고 오탐이 방해가 된다"며 뺐다. 실사용 데이터를
보고 정하기로 했다 — 지금 그 데이터가 쌓였다.

---

## C. 품질 부채

### C1. 회수 사각 — ✅ 2026-08-14

**여기 적힌 숫자가 낡았었다.** 406건 중 93건이 아니라 245건 중 결정 147 ·
비결정 98이었다 (사각 40%).

`summary` 를 단 비결정 문서를 회수 대상에 넣고, **참고에 자리를 따로 줬다**
(결정 3 · 참고 2). 섞어 자르면 참고가 결정을 밀어낸다 — 실측 15%.
볼트의 참고 문서 91건에 요약을 달았다.

상세: `priorcase-결정-참고문서를회수에넣되-summary가참여신호-2026-08-14`
교훈: `common-결정-대리지표로재고-실물로안재면-낙관이된다-2026-08-14`

### C2. 미뤄둔 리뷰 지적

**어디에도 기록돼 있지 않다** — 코드에 표시가 없고 볼트에도 없다.
이 문서가 유일한 기록이 된다.

- ~~체크포인트 무한 증가~~ — ✅ 2026-08-13. 사라진 파일의 **빈 항목만** 지운다.
- ~~`startupPass` 가 `At` 이 아니라 `Offset != 0` 으로 판정~~ — 고치려다 **되돌렸다.**
  판정을 바꾸면 임계 미만 파일이 영구 재훑기가 되어 예산을 소진한다 (아래 참고).
- ~~원장 쓰기가 어댑터에 산다 (계층 규칙 위반 소지)~~ — ✅ 2026-08-14. 재 보니
  **위반이 아니었다** (어댑터의 직접 호출은 시험 픽스처뿐). 소지를 막는 것이
  없어서 `TestOnlyDaemonWritesState` 로 못박았다.
- 그 밖 3건 (상세 유실 — 리뷰를 다시 돌려야 한다)

#### C2-a. 누적 발화 수를 체크포인트에 — ✅ 2026-08-13

`Scan` 은 임계 미만이면 **전진하지 않는다** — 발화를 누적하려는 설계다. 그 대가로
두 결함이 붙어 있다.

1. **0바이트 소비 파일이 시딩된다.** `Offset != 0` 이 "아는 파일" 의 기준이라,
   훑었지만 아무것도 소비 못 한 파일이 "처음 보는 파일" 로 분류돼 끝으로 시딩된다.
   그 사이 대화가 통째로 사라진다.
2. **판정을 고치면 트레드밀이 생긴다.** 항목의 존재로 판정하면 임계 미만 파일이
   영원히 재훑기 집합에 남는다 (`Behind` 가 늘 true). 실측: 임계 미만 비율이
   Claude Code 68.7% · Codex 92.2%, 9일치 시뮬레이션에서 10초 예산 소진 ·
   못 본 파일 2,165~2,513 · 상태 파일 1.10MB (현재 코드는 320ms · 0건).

**둘 다 같은 뿌리다.** 체크포인트가 누적 발화 수를 들면, 임계 미만이어도 offset 을
전진시키면서 발화를 누적할 수 있어 동시에 풀린다. 상세는 볼트
`priorcase-결정-체크포인트정리-중립성만-재설계는되돌림-2026-08-13`.

#### C2-b. 데몬 `startupPass` 의 drain 에 예산이 없다 — ✅ 2026-08-14

매 기동 29초 · 상태 파일 3,417회 재작성. 데몬을 쓰는 사용자에게 걸리는 값이다.

#### C2-c. 서브에이전트 기록이 Claude Code 파일의 80% — ✅ 2026-08-14

1,369/1,713, 하루 150~185개 증가. 지금은 시딩으로 넘어가 무해하지만, 훑기 대상이
되는 순간 한 대화가 파일 수만큼 중복 표시된다.

### C3. 커버리지 — ✅ 2026-08-14

**여기 적힌 진단이 틀렸었다.** "rollup 14.8% · doctor 11.1% · 본체는 실제 구멍"
은 `go test` 의 패키지별 값이었다. `-coverpkg` 로 재니 전체 **83.6%** 이고 진짜
0% 는 9개뿐인데 7개가 cobra 배선이다. rollup·doctor 의 본체는 76~100% 로 이미
덮여 있었다.

본체에서 진짜 구멍은 `checkJudge`(44.4%) 하나였고, 다섯 갈래를 전부 못박아
100% 로 올렸다. 전체 83.9%.

### C4. CLI 진단 문구 국제화

의도적으로 범위 밖에 뒀다 (에이전트가 읽는 표면만 i18n). 외부 사용자가 생기면
필요해진다 — 내부용이면 보류해도 된다.

### C5. ③ 자동 승격 관찰

컷오버 후 실제로 노트를 만든 것이 **1건**이고, 그 1건에 결함이 둘 있었다
(근거 날조·날짜 2일 오차). 둘 다 고쳤지만 **지시문 제약은 완화이지 보장이 아니다.**
`promotions.jsonl` 의 `rec=true` 를 당분간 사람이 읽어야 한다.

---

## D. 잡일

- `~/project/casebook` 디렉토리 이름 변경 — **절차와 실측 근거를
  `docs/rename-to-priorcase.md` 에 준비해 뒀다.** 세션을 닫은 뒤 실행한다
  (살아 있는 세션의 cwd 를 밑에서 바꾸면 조용하게 틀린다).
- 플랫폼 패키지 deprecate — **명령과 대상을 `docs/npm-deprecate.md` 에 준비해
  뒀다.** 건마다 2FA 라 사람이 돌린다 (총 9회).
- ~~미확인 구간 19건 정리~~ — 2026-08-14 기준 남은 것은 nova 세션 몫이다
  (그 프로젝트 맥락이 있는 세션에서 판정한다).

---

## 권고 순서

1. ~~**A 묶음** (A1·A2·A3)~~ — ✅ 2026-08-12 완료.
2. ~~**B1** 호스트 파서~~ — Codex ✅ 2026-08-12. Gemini·Cursor 는 설계가 선행.
3. **B3** 데스크탑 앱 (설치·배선·상태) — 음성 없이. 온보딩 마찰 제거.
4. **남은 구멍 실측** — 사람 업무가 얼마나 안 잡히는지 숫자로 본다. B4·B5 는 이 뒤다.
5. **A4** 볼트 분리 · **C1** 회수 사각 — 둘 다 설계 결정이 선행.
6. C2 리뷰 지적 · C3 커버리지 · D 잡일.

**지켜야 할 선**: 소스를 늘리되 "전부 색인" 이 아니라 "결정을 잡는다" 를 유지한다.
그 선을 넘는 순간 해자가 데이터 접근 + 랭킹으로 바뀌고, 그건 예산이 훨씬 큰
회사들의 싸움이다. 상세는 볼트 `priorcase-결정-제품범위확장-업무옆에서돕는층-2026-08-12`.
