{
  "name": "priorcase-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "bash scripts/bundle-prior.sh && tsc --noEmit && vite build",
    "test": "vitest run",
    "tauri": "tauri",
    "sidecar": "bash scripts/bundle-prior.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.0",
    "@types/node": "^26.2.0",
    "jsdom": "^30.0.1",
    "typescript": "^5.9.0",
    "vite": "^7.1.0",
    "vitest": "^3.2.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.9.0"
  }
}
